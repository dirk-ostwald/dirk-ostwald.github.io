Aenderungsliste zu 208-Kontrollstrukturen.qmd
Erstellt am 2026-05-06

Diese Liste dokumentiert die von Codex vorgenommenen Aenderungen an
208-Kontrollstrukturen.qmd.

1. Sprache, Grammatik und Stil
- "Perse" wurde zu "Per se" korrigiert.
- "`if()` und `switch()` Statement" wurde zu
  "`if()`- und `switch()`-Statements" korrigiert.
- "`for()`, `while()`, und `repeat()` Schleifen" wurde zu
  "`for()`-, `while()`- und `repeat()`-Schleifen" korrigiert.
- "Kontrollstrutkuren" wurde zu "Kontrollstrukturen" korrigiert.
- "spezifieren" wurde zu "spezifizieren" korrigiert.
- "Gleicheit" wurde zu "Gleichheit" korrigiert.
- "gfoesser" wurde zu "groesser" korrigiert.
- "kombinierte if-else-statements werden leicht unuebersichtlich" wurde zu
  "kombinierte if-else-statements leicht unuebersichtlich" korrigiert.
- "if-statemen saequivalent" wurde zu "if-statement aequivalent" korrigiert.
- "bezug nehmen" wurde zu "Bezug nehmen" korrigiert.
- "Dimensionserwatungsunterschiede" wurde zu
  "Dimensionserwartungsunterschiede" korrigiert.
- "Varialben" wurde zu "Variablen" korrigiert.
- "Iternationsindices" wurde zu "Iterationsindizes" korrigiert.
- "wiederrum" wurde zu "wiederum" korrigiert.

2. Code-Konsistenz
- Chunk-Optionen wurden von `echo = T/F` und `eval = F` zu
  `echo = TRUE/FALSE` und `eval = FALSE` vereinheitlicht.
- Im `switch()`-Beispiel mit `x = "a"` wurde der Kommentar von `x = 2` zu
  `x = "a"` korrigiert.
- Der doppelte Ausdruck "wird der Befehl ... wird" wurde korrigiert.
- In den Matrixbeispielen wurde `nrow = 4` zu `nrow = n` geaendert.
- "Matrixpreallokation" wurde zu "Matrixpraeallokation" korrigiert.
- Im zweiten geschachtelten Schleifenbeispiel wurde der Text korrigiert:
  Die Elemente entsprechen dort den Zeilenindizes, nicht den Spaltenindizes.

3. Technische Bereinigung
- Trailing whitespace wurde entfernt.
