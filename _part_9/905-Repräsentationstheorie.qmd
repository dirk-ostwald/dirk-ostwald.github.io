# Repräsentationstheorie {#sec-repräsentationstheorie}

Die Grundidee des Messens ist es, Eigenschaften von Objekten Zahlen so zuzuweisen, dass
die Relationen der Eigenschaften der Objekte sowie Operationen mit Objekteigenschaften
im Bereich der Zahlen erhalten bleiben.

:::{#exm-messen-von-entscheidungspräferenzen}
## Messen von Entscheidungsoptionspräferenzen

Messen der Entscheidungsoptionspräferenzen eines Agenten bedeutet,
den Entscheidungsoptionen Zahlen so zuzuordnen, dass die qualitative Relation
"Entscheidungsoption $m$ wird präferiert über Entscheidungsoption $n$" in der
Realität im Bereich der Zahlen erhalten bleibt.

Sei $M$ die Menge von Entscheidungsoptionen und sei $R$ die Entscheidungsoptionspräferenzrelation,
d.h. für $m,n \in M$ gelte
\begin{equation}
m \mbox{ wird präferiert über } n \Leftrightarrow (m,n) \in R .
\end{equation}
Dann möchte man bei der Messung von Entscheidungsoptionspräferenzen $m$ und $n$
so mithilfe einer Funktion $f$ Zahlen zuordnen, dass gilt
\begin{equation}
m \mbox{ wird präferiert über } n \Leftrightarrow (m,n) \in R \Leftrightarrow f(m) > f(n).
\end{equation}
Eine solche Funktion heißt dann *Nutzenfunktion (utility function)*. Das Messen von
Entscheidungsoptionspräferenzen wird in der Folge unser Beispiel für den Begriff
der *Ordinalmessung* und den zugehörigen Begriff der *Ordinalskala* sein.
:::

:::{#exm-messen-von-entscheidungspräferenzen}
## Messen der Masse von Objekten

Messen der Masse eines Objektes bedeutet, den Objekten Zahlen so zuzuordnen, dass die
qualitative Relation "Objekt $m$ ist schwerer als Objekt $n$" in der Realität im Bereich der
Zahlen erhalten bleibt. Weiterhin möchte man  beim Messen der Masse eines Objektes
auch sicherstellen, dass der Messprozess additiv ist, d.h. dass die Masse der
physischen Kombination zweier Objekte der Summation der Massen der einzelnen
Objekte entspricht.

Sei $M$ die Menge von Objekten, sei $R$ die Gewichtsvergleichrelation, d.h. für
$m,n \in M$ gelte
\begin{equation}
m \mbox{ ist schwerer als } n \Leftrightarrow (m,n) \in R
\end{equation}
und $\circ$ sei die Operation des physischen Kombinierens zweier Objekte, d.h.
für $m,n \in M$ gelte
\begin{equation}
m \mbox{ wird kombiniert mit } n \Leftrightarrow m \circ n.
\end{equation}
Dann möchte man bei der Messung der Masse von Objekten $m$ und $n$ so mit Hilfe
einer Funktion $f$ Zahlen zuordnen, dass gilt
\begin{equation}
m \mbox{ ist schwerer als } n \Leftrightarrow (m,n) \in R \Leftrightarrow f(m) > f(n)
\end{equation}
und
\begin{equation}
f(m \mbox{ wird kombiniert mit } n) \Leftrightarrow f(m \circ n) = f(m) + f(n).
\end{equation}
Eine solche Funktion heißt *Waage*. Das Messen der Masse von Objekten wird in
der Folge unser Beispiel für den Begriff der *Extensivmessung* und den zugehörigen
Begriff der *Verhältnisskala* sein.
:::


Axiome der Repräsentationstheorie = Bedingungen an qualitative
Relationssysteme für ihre numerische Repräsentation = Die qualitativein
Relationssysteme müssen bestimmte Bedingungen erfüllen, um numerisch
repräsentiert werden zu können. Wir verstehen diese Bedingungen hier als
(qualitative) Datenmuster, die prinzipiell empirisch bestimmbar sind, meist,
in dem man alle möglichen paarweisen Vergleiche der zu messenden
(Objekt)eigenschaften bestimmt.


\footnotesize

: Skalenarten nach Tabelle 1 in  @stevens1946 {#tbl-skalenarten-nach-stevens}

| Skala      | Empirische Operationen      | Gruppenstruktur          | Zulässige Statistiken   |
|:-----------|:----------------------------|:-------------------------|:------------------------|
| NOMINAL    | Feststellung der Gleichheit | Permutationsgruppe       | Anzahl an Fällen        |
|            |                             | $x'= f(x)$               | Modus                   |
|            |                             | $f(x)$ bijektiv          | Kontingenzkorrelation   |
| ORDINAL    | Feststellung von größer     | Isotonische Gruppe       | Median                  |
|            | oder kleiner                | $x'= f(x)$               | Perzentile              |
|            |                             | $f(x)$ monoton steigend  |                         |
| INTERVALL  | Feststellung der Gleichheit | Generelle lineare Gruppe | Mittelwert              |
|            | von Intervallen oder        | $x'= ax + b$             | Standardabweichung      |
|            | Differenzen                 |                          | Rangkorrelation         |
|            |                             |                          | Korrelation             |
| VERHÄLTNIS | Feststellung der Gleichheit | Ähnlichkeitsgruppe       | Variationskoeffizient   |
|            | von Verhältnissen           | $x' = ax$                |                         |

\normalsize



## Relationen, Operationen und Relationssysteme {#sec-relationen}

### Relationen {-}

:::{#def-n-stellige-relation}
## $n$-stellige Relation
Gegeben seien $n$ Mengen $M_1,...,M_n$. Dann ist eine *$n$-stellige Relation
$R$ auf $M_1,...,M_n$* definiert als eine Teilmenge des Kartesischen Produkts
von $M_1,...,M_n$,
\begin{equation}
R \subseteq \prod_{i=1}^n M_i := M_1 \times \cdots \times M_n := \{(m_1,...,m_n)|m_i \in M_i, i = 1,...,n\}.
\end{equation}
:::

Eine $n$-stellige Relation ist also eine Menge von geordneten $n$-Tupeln.
Für zwei Mengen $M_1$ und $M_2$ ist eine $2$-stellige Relation $R$ eine Menge von geordneten Paaren.
\begin{equation}
R = \{(m_1,m_2)|m_1 \in M_1,m_2 \in M_2\} \subset M_1 \times M_2.
\end{equation}
Für drei Mengen $M_1, M_2$ und $M_3$ ist eine $3$-stellige Relation $R$
eine Menge von geordneten Tripeln
\begin{equation}
R = \{(m_1,m_2,m_3)|m_1 \in M_1, m_2 \in M_2, m_3 \in M_3\} \subset M_1 \times M_2 \times M_3.
\end{equation}
Dabei sind die Mengen $M_1,...,M_n$ nicht notwendig verschieden. Eine zweistellige
Relation auf $M_1 \times M_2$ mit $M_1 = M_2$, also $M_1 \times M_2 = M^2$ heißt
*Binärrelation auf $M$*. Wir betrachten in der Folge Binärrelationen genauer.

:::{#def-binärrelation}
## Binärrelation
$M$ sei eine Menge. Dann heißt eine Teilmenge $R$ des Kartesischen Produkts $M \times M$
eine *Binärrelation auf $M$*. Wenn für ein (geordnetes Paar) $(m,n) \in M \times M$
gilt, dass $(m,n) \in R$ ist, so schreiben wir $mRn$ und bezeichnen.
:::

Es gilt also durchgängig
\begin{equation}
(m,n) \in R \Leftrightarrow mRn.
\end{equation}
Die Schreibweise $mRn$ soll darauf hinweisen, dass Relationen wie zum Beispiel
"größer als" oft zwischen den Symbolen $m$ und $n$ in der Form
\begin{equation}
m > n
\end{equation}
geschrieben werden. In $mRn$ tritt also die allgemeine Relation $R$ an die Stelle
des $>$.


:::{#exm-binärrelation-auf-einer-endlichen-menge}
## Binärrelationen auf einer endlichen Menge
Es sei $M := \{a,b,c\}$ eine Menge. Dann ist
\begin{equation}
M \times M = \{(a,a), (a,b), (a,c), (b,a), (b,b), (b,c), (c,a), (c,b), (c,c)\}
\end{equation}
Jede Teilmenge von $M \times M$ ist eine Binärrelation auf $M$. Zum Beispiel ist also
\begin{equation}
R := \{(a,a), (b,b), (c,c)\}
\end{equation}
eine Binärrelation auf $M$. Man schreibt hier also $aRa$, $bRb$, $cRc$,
aber auch zum Beispiel $a\not Rb$. Eine andere Relation auf $M$ ist
\begin{equation}
R := \{(a,b), (b,c), (a,c)\}
\end{equation}
Man schreibt hier entsprechend $aRb$, $bRc$, $aRc$,
aber zum Beispiel auch $a\not Ra$.
:::

:::{#exm-binärrelation-auf-R}
## Binärrelationen auf $\mathbb{R}$

Es sei $\mathbb{R}$ die Menge der reellen Zahlen. Dann ist die Menge definiert durch
\begin{equation}
R := \{(x,y)|x \ge y \mbox{ und } x,y \in \mathbb{R}\} \subset \mathbb{R}^2
\end{equation}
eine Binärrelation auf $\mathbb{R}$. Wir nennen diese Relation die $\ge$ (größer-gleich) Relation.
:::

```{r,echo = F, eval = F}
library(latex2exp)
pdf(
file         = "./_figures/905-binärrelation.pdf",
width        = 5,
height       = 5)
par(
family      = "sans",
mfcol       = c(1,1),
pty         = "s",
bty         = "l",
lwd         = 1,
las         = 1,
mgp         = c(2,1,0),
xaxs        = "i",
yaxs        = "i",
font.main   = 1,
cex         = 1,
cex.main    = 1)

plot(
seq(-2,2,length=1e1),
seq(-2,2,length=1e1),
xlim = c(-2,2),
ylim = c(-2,2),
xlab = "",
ylab = "",
type = "l",
axes = F)
axis(1, pos=0)
axis(2, pos=0)

polygon(
c(-2,2,2),
c(-2,-2,2),
col=adjustcolor("gray", alpha= .2),
border = NA)

text( 0.0, 2.2, TeX("$y$") , cex = 1, xpd = T)
text( 2.2, 0.0, TeX("$x$") , cex = 1, xpd = T)
text(-1.2, 1.2, TeX("$x<y"), cex = 1.2)
text( 1.2,-1.2, TeX("$x>y"), cex = 1.2)
legend(.5,.5, TeX("$x = y$") , lty = 1,col = "black", bty = "n", cex = 1.2)

dev.off()
```


![Binärrelation auf $\mathbb{R}$.](./_figures/905-binärrelation.pdf){#fig-binärrelation fig-align="center" width=60%}

### Ausgewählte Eigenschaften {-}

:::{#def-reflexivität}
## Reflexivität

Eine Binärrelation $R$ auf eine Menge $M$ heißt *reflexiv*, wenn für
jedes $m \in M$ gilt, dass $(m,m)\in R$ ist, wenn also $mRm$ gilt.
:::


:::{#exm-reflexivität-diskreter-relationen}
## Reflexivität diskreter Relationen
Für $M := \{a,b\}$ ist
\begin{equation}
R := \{(a,a), (a,b), (b,a), (b,b)\}
\end{equation}
eine reflexive Relation auf $M$, weil für $a \in M$ und $b \in M$ gilt, dass $(a,a)$ und $(b,b)$ in $R$ sind. Für $M := \{a,b\}$ ist
\begin{equation}
R := \{(a,a), (a,b), (b,a)\}
\end{equation}
keine reflexive Relation auf $M$, weil $(b,b)\notin R$.
:::

:::{#exm-reflexivität-der-ge-relation}
## Reflexivität der $\ge$-Relation

Die $\ge$ Relation auf $\mathbb{R}$ is reflexiv, weil für alle $x \in \mathbb{R}$
gilt, dass $x \ge x$.
:::

:::{#def-symmetrie}
## Symmetrie

Eine Binärrelation $R$ auf einer Menge $M$ heißt *symmetrisch*, wenn für alle
$(m,n)\in R$ folgt, dass auch $(n,m)\in R$ ist, wenn also aus $mRn$ folgt,
dass $nRm$ gilt.
:::


:::{#exm-symmetrie-diskreter-relationen}
## Symmetrie diskreter Relationen

Für $M := \{a,b\}$ ist
\begin{equation}
R := \{(a,b), (b,a)\}
\end{equation}
eine symmetrische Relation auf $M$, weil
\begin{align}
\begin{split}
(a,b) \in R & \Rightarrow (b,a) \in R \\
(b,a) \in R & \Rightarrow (a,b) \in R \\
\end{split}
\end{align}
Für $M := \{a,b\}$ ist
\begin{equation}
R := \{(a,a), (a,b), (b,b)\}
\end{equation}
keine symmetrische Relation auf $M$, weil $(a,b) \in R$, aber $(b,a) \notin R$.
:::

:::{#exm-nicht-symmetrie-der-ge-relation}
## Nicht-Symmetrie der $\ge$-Relation
Die $\ge$ Relation auf $\mathbb{R}$ ist nicht symmetrisch, weil aus $x \ge y$
nicht notwendigerweise folgt, dass $y \ge x$: es kann ja auch $x>y$ gelten, dann gilt
$y \ge x$ aber nicht.
:::

:::{#def-transitivität}
## Transitivität
Eine Binärrelation $R$ auf einer Menge $M$ heißt *transitiv*, wenn für alle
$m,n,p\in M$ gilt, dass aus $(m,n)\in R$ und $(n,p)\in R$ folgt, dass $(m,p) \in R$ ist,
wenn also aus $mRn$ und $nRp$ folgt, dass $mRp$ gilt.
:::

:::{#exm-transitivität-diskreter-relationen}
Auf $M := \{a,b,c\}$ ist
\begin{equation}
R := \{(a,b), (b,c), (a,c)\}
\end{equation}
eine transitive Relation, weil neben $(a,b) \in R$ und $(b,c) \in R$ auch $(a,c) \in R$ gilt.
Für $M := \{a,b,c\}$ ist
\begin{equation}
R := \{(a,b), (b,c)\}
\end{equation}
keine transitive Relation auf $M$, weil trotz $(a,b) \in R$ und $(b,c) \in R$ nicht $(a,c) \in R$ gilt.
:::

:::{#exm-transitivität-der-ge-relation}
Die $\ge$ Relation auf $\mathbb{R}$ ist transitiv, denn aus $x \ge y$ und $y \ge z$ folgt, dass $x \ge z$.
:::

:::{#def-irreflexivität}
## Irreflexivität

Eine Binärrelation $R$ auf eine Menge $M$ heißt *irreflexiv*, wenn für
jedes $m \in M$ gilt, dass $(m,m)\notin R$ ist, wenn also $m\not Rm$ gilt.
:::


* Für $M := \{a,b\}$ ist
\begin{equation}
R := \{(a,b), (b,a)\}
\end{equation}
eine irreflexive Relation auf $M$, weil für $a \in M$ und $b \in M$ gilt, dass $(a,a)\notin R$ und $(b,b)\notin R$ sind.


:::{#exm-irreflexivität-der-g-relation}
## Irreflexivität der $>$-Relation
Die $>$ Relation auf $\mathbb{R}$ ist irreflexiv, denn für kein $x \in \mathbb{R}$
gilt, dass $x > x$.
:::


Irreflexiv und nicht reflexiv sind nicht identisch: eine Relation ist schon
dann nicht reflexiv, wenn es nur ein $m \in M$ mit $(m,m)\notin R$ gibt,
allerdings ist sie nur dann irreflexiv, wenn dies tatsächlich für alle $m \in M$ gilt.
Relationen können also weder reflexiv noch irreflexiv sein.

:::{#exm-nicht-irreflexive-nicht-reflexive-relation}
## Nicht-irreflexive, nicht-reflexive Relation
Sei zum Beispiel $M := \mathbb{R}^2$ und
\begin{equation}
R := \{(x,y)|y = x^2\}
\end{equation}
Dann gilt $(1,1) \in R$, weil $1 = 1^2$, aber $(2,2) \notin R$, weil $2 \neq 2^2$.
Also ist $R$ auf $\mathbb{R}^2$ weder reflexiv noch irreflexiv.
:::


### Operationen {-}

:::{#def-relationsdefinition-von-funktionen}

## Relationsdefinition von Funktionen
$D$ und $Z$ seien Mengen. Eine Binärrelation $f$ auf $D \times Z$ heißt
*Funktion (oder Abbildung) von $D$ nach $Z$*, wenn $f$ folgende Eigenschaften hat:
1. Für alle $x \in D$ gibt es ein $y \in Z$, so dass $(x,y) \in f$.

1. Wenn $(x,y)\in f$ und $(x,y') \in f$ sind, so folgt $y = y'$.
:::

Eine Funktion wird hier betrachtet als eine Menge von geordneten Paaren
\begin{equation}
f := \{(x,y)|x\in D, y\in Z\} \subseteq D \times Z
\end{equation}
Die erste der in @def-relationsdefinition-von-funktionen definierenden Eigenschaften
einer Funktion besagt dann, dass eine Funktion jedem Element in $D$ mindestens
ein Element in $Z$ zuordnet. Die zweite Eigenschaft besagt, dass eine Funktion
jedem Element in $D$ höchstens ein Element in $Z$ zuordnet. Zusammen besagen beide
Eigenschaften also, dass eine Funktion jedem $x \in D$ genau ein $y \in Z$ zuordnet,
was unsere Definition von Funktionen in @sec-funktionen entspricht.
@def-relationsdefinition-von-funktionen definiert Funktionen als Mengen,
vermeidet also den den undefinierten Begriff der "Vorschrift" in @def-funktion
und gibt der Mathematik somit eine rein mengentheoretische Grundlage.

Unsere Notation von Funktionen bleibt von der Relationsdefinition von Funktionen
unberührt, wir schreiben weiterhin $f : D \to Z$ dafür, dass $f$ eine Funktion
(Abbildung) von $D$ nach $Z$ ist und bezeichnen das zu $x \in D$ eindeutig bestimmte
$y \in Z$ als $f(x):=y$.

Operationen sind spezielle Funktionen:

:::{#def-operation}
## Operation

$M$ sei eine Menge. Eine *Operation (oder Verknüpfung)* $\circ$ auf $M$ ist eine
Funktion der Form
\begin{equation}
\circ : N \subseteq M \times M \to M, (m,n) \mapsto \circ((m,n)).
\end{equation}
:::

Die Einschränkung von $M \times M$ auf $N \subseteq M \times M$ dient dem
Realitätsanspruch extensiver Strukturen, wie wir an späterer Stelle sehen werden
(vgl. @krantz1971, S. 81-82). Im numerischen Kontext ist meist $N = M \times M$.

:::{#exm-operationen}
## Operationen

1. Eine vertraute Operation ist die Addition natürlicher Zahlen
\begin{equation}
\mbox{$+$}: \mathbb{N} \times \mathbb{N} \to \mathbb{N}, (m,n) \mapsto +((m,n)) := m + n.
\end{equation}
1. Eine andere vertraute Operation ist die Multiplikation ganzer Zahlen,
\begin{equation}
\cdot : \mathbb{Z} \times \mathbb{Z} \to \mathbb{Z}, (m,n) \mapsto \cdot((m,n)) := mn.
\end{equation}
1. Für eine Menge $N$ und die Menge
\begin{equation}
M := \{f : N \to N| f \mbox{ ist eine Funktion}\}
\end{equation}
ist die Verknüpfung von Funktionen eine Operation auf $M$,
\begin{equation}
\circ : M \times M \to M, (f,g) \mapsto \circ((f,g)) := f \circ g \mbox{ mit } (f \circ g)(n) := f(g(n)) \mbox{ für alle } n \in N.
\end{equation}
:::

Eine Operation ist eine 3-stellige Relation $R$ auf einer Menge $M$ der Form
\begin{equation}
R = \{(m,n, m\circ n)|m,n,m\circ n \in M\} \subseteq M \times M \times M
\end{equation}
Allerdings ist nicht jede 3-stellige Relation $R$ auf einer Menge $M$ eine Operation.

:::{#exm-drei-stellige-relationen-und-operationen}
## 3-stellige Relationen und Operationen

Sei zum Beispiel $M := \{1,2,3\}$. Dann entspricht die Operation
\begin{equation}
\circ : M \times M \to M,
(m,n) \mapsto \circ((m,n)) :=
\begin{cases}
1 & \mbox{ wenn } a + b \mbox{ gerade ist} \\
2 & \mbox{ wenn } a + b \mbox{ ungerade ist}
\end{cases}.
\end{equation}
der 3-stelligen Relation
\begin{equation}
R = \{(1,1,1), (1,2,2), (1,3,1), (2,1,2), (2,2,1), (2,3,2), (3,1,1), (3,2,2), (3,3,1)\}
\end{equation}
Sei andererseits $M := \{1,2,3\}$ und die 3-stellige Relation $R$ definiert als
\begin{equation}
R := \{(1,1,1), (1,2,2), (1,3,1), (2,1,2), (2,2,1), (2,3,2), (3,1,1), (3,2,2), (1,3,2)\}.
\end{equation}
Dann gilt sowohl $(1,3,1) \in R$ und $(1,3,2) \in R$, also in Operationschreibweise
\begin{equation}
(1,3) \mapsto \circ((1,3)) := 1 \mbox{ und } (1,3) \mapsto \circ((1,3)) := 2.
\end{equation}
Dann ist $\circ$ aber keine Funktion, weil eine Funktion jedem Element der Definitionsmenge
nur höchstens ein Element der Zielmenge zuordnet und nicht zwei.
:::

### Relationssysteme {-}

:::{#def-relationssystem}
## Relationssystem

$M$ sei eine Menge, $R_1,...,R_p$ seien Relationen auf $M$ und $\circ_1,...,\circ_q$
seien Operationen auf $M$. Dann wird das $(1 + p + q)$-Tupel
\begin{equation}
\mathcal{M} := (M,R_1,...,R_p, \circ_1,...,\circ_q)
\end{equation}
ein *Relationssystem* genannt. Für $M := \mathbb{R}$ heißt ein Relationssystem
ein *numerisches Relationssystem*. Der *Typ eines Relationssystems*
ist eine (endliche) Folge $(r_1,...,r_p,q)$ der Länge $p+1$, für die gilt, dass für alle
$i = 1,...,p$ $r_i$ gleich $\rho$ ist, wenn $R_i$ eine $\rho$-stellige Relation ist.
:::

Da $\circ_1,...,\circ_q$ Binärrelationen sind, kann man auch
\begin{equation}
\mathcal{M} := (M,R_1,...,R_{p+q})
\end{equation}
schreiben.
Der Relationstyp $(2,3,2)$ eines Relationssystems $\mathcal{M}$ besagt, dass
auf der Menge $M$ eine 2-stellige und eine 3-stellige Relation, sowie zwei Operationen definiert sind.

:::{#exm-beispiele-für-qualitative-relationssysteme}
## Qualitative Relationssysteme

1. $M$ sei eine Menge von Objekten und es sei
\begin{equation}
R := \{(m,n)| m \mbox{ ist wärmer als } n\} \subseteq  M\times M.
\end{equation}
Dann ist $\mathcal{M} := (M,R)$ ein Relationssystem vom Typ $(2,0)$.
1. $M$ sei eine Menge von Entscheidungsoptionen und es sei
\begin{equation}
R := \{(m,n)| m \mbox{ wird präferiert über } n\} \subseteq  M\times M
\end{equation}
Dann ist $\mathcal{M} := (M,R)$ ein Relationssystem vom Typ $(2,0)$.
1. $M$ sei eine Menge von Objekten und es sei
\begin{equation}
R := \{(m,n)| m \mbox{ ist schwerer als } n\} \subseteq  M\times M
\end{equation}
und
\begin{equation}
\circ : M \times M \to M, (m,n) \mapsto \circ((m,n)) := \mbox{Die physische Kombination von } m \mbox{ und } n.
\end{equation}
Dann ist $\mathcal{M} := (M,R,\circ)$ ein Relationssystem vom Typ $(2,1)$.

:::{#exm-beispiele-für-numerische-relationssystem}
## Numerische Relationssysteme

1. $\mathcal{N} := (\mathbb{R},>)$ ist ein numerisches Relationsystem vom Typ (2,0).
1. $\mathcal{N} := (\mathbb{R},>,\ge)$ ist ein numerisches Relationsystem vom Typ (2,2,0).
1. $\mathcal{N} := (\mathbb{R},>,\ge, +)$ ist ein numerisches Relationsystem vom Typ (2,2,1).
1. $\mathcal{N} := (\mathbb{R},>,\ge, +, \cdot)$ ist ein numerisches Relationsystem vom Typ (2,2,2).
:::

## Homomorphismen, Isomorphismen und Skalen {#sec-homomorphismen-isomorphismen-und-skalen}

:::{#def-homomorphismus}
## Homomorphismus

$\mathcal{M} := (M,R_1,...,R_p, \circ_1, ...,\circ_q)$ und
$\mathcal{N} := (N,\tilde{R}_1,...,\tilde{R}_p, \tilde{\circ}_1, ...,\tilde{\circ}_q)$
seien zwei Relationssysteme gleichen Typs. Dann heißt eine Funktion $f : M \to N$
ein *Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$*, wenn $f$ folgende
Eigenschaften hat:

1. Für alle $i = 1,...,p$ und alle $m_1,...,m_{r_i} \in M$ gilt
\begin{equation}
(m_1,...,m_{r_i}) \in R_i \Leftrightarrow (f(m_1),...,f(m_{r_i})) \in \tilde{R}_i.
\end{equation}
1. Für alle $i = 1,...,q$ und alle $m,n\in M$ gilt
\begin{equation}
f(m \circ_i n) = f(m) \tilde{\circ}_i f(n).
\end{equation}

Wenn zwischen zwei Relationssystemen $\mathcal{M}$ und $\mathcal{N}$ gleichen Typs
ein Homomorphismus existiert, dann heißen $\mathcal{M}$ und $\mathcal{N}$ *homomorph*.
:::

Die Definition eines Homomorphismus basiert also auf der Existenz zweier
Relationssysteme gleichen Typs mit endlichen vielen Relationen und Operationen.
In der Repräsentationstheorie ist $\mathcal{M}$ meist ein qualitatives Relationssystem
und $\mathcal{N}$ ein numerisches Relationssystem. Die erste Eigenschaft in
@def-homomorphismus besagt, dass wenn ein $r_i$-Tupel von Elementen in $M$ ein
Element der $i$ten Relation $R_i$ auf $M$ ist, dann soll das $r_i$-Tupel der durch
$f$ transformierten Elemente ein Element der $i$ten Relation $\tilde{R}_i$ auf
$N$ sein und umgekehrt. Die zweite Eigenschaft von @def-homomorphismus besagt,
dass für $i = 1,...,q$ die durch $f$ transfomierte $i$te Verknüpfung zweier
Elemente aus $M$ auf $M$ gleich der $i$ten Verknüpfung der durch $f$ transfomierten
Elemente aus $M$ auf $N$ sein soll.

:::{#exm-homomorphismus-beim-messen-von-entscheidungspräferenzen}

## Homomorphismus beim Messen von Entscheidungspräferenzen
Im Fall des Messen von Entscheidungsoptionspräferenzen betrachtet man $\mathcal{M} = (M,R)$
und $\mathcal{N} = (\mathbb{R},>)$. Ein Homomorphismus ist mit $p := 1, r_1 := 2$
und $q:=0$ dementsprechend eine Funktion $f$, für die gilt
\begin{equation}
(m,n) \in R \Leftrightarrow f(m) > f(n).
\end{equation}
:::

:::{#exm-homomorphismus-beim-messen-von-objektmassen}

## Homomorphismus beim Messen von Objektmassen
Im Fall des  Messen der Masse von Objekten betrachtet man $\mathcal{M} = (M,R,\circ)$
und $\mathcal{N} = (\mathbb{R},>,+)$. Ein Homomorphismus  und fragt mit $p := 1,r_1 := 2$ und $q:=1$
dementsprechend nach einer Funktion $f$, für die gilt
\begin{equation}
(m,n) \in R \Leftrightarrow f(m) > f(n)
\mbox{ und }
f(m \circ n) = f(m) + f(n).
\end{equation}
:::

Speziallfälle von Homomorphismen sind sogenannte *Isomorphismen*.

:::{#def-isomorphismus}
## Isomorphismus
Ein injektiver Homomorphismus heißt *Isomorphismus*. Wenn zwischen zwei
Relationssystemen $\mathcal{M}$ und $\mathcal{N}$ gleichen Typs ein Isomorphismus
existiert, dann heißen $\mathcal{M}$ und $\mathcal{N}$ *isomorph*.
:::

Für einen Isomorphismus gilt neben den definierenden Eigenschaften aus @def-homomorphismus
also außerdem, dass $f$ injektiv ist, also dass für $m,n\in M$ gilt, dass
\begin{equation}
m \neq n \Rightarrow f(m) \neq f(n) \mbox{ bzw. } f(m) = f(n) \Rightarrow m = n.
\end{equation}

Folgendes Beispiel zeigt, dass Homomorphismen nicht eindeutig sein müssen, es kann
also mehr als einen Homomorphismus zwischen einem qualitativem und einem numerischen
Relationssystem geben.

:::{#exm-existenz-und-nicht-eindeutigkeit-eines-homomorphismus}
## Existenz und Nicht-Eindeutigkeit eines Homomorphismus

Es sei $\mathcal{M} := (M,R)$ das $(2,0)$ Relationssystem mit $M := \{a,b,c,d\}$ und
\begin{equation}
R := \{(a,b), (b,c), (a,c), (a,d), (b,d), (c,d)\}
\end{equation}
Weiterhin sei $\mathcal{N} := (\mathbb{R},>)$ ein numerisches Relationssystem. Dann ist
\begin{equation}
f : M \to \mathbb{R}, m \mapsto f(m) \mbox{ mit }
f(a) := 4, f(b) := 3, f(c) := 2, f(d) := 1
\end{equation}
ein Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$, weil
\begin{align}
\begin{split}
(a,b) \in R \Leftrightarrow aRb  & \Leftrightarrow f(a) > f(b) \Leftrightarrow 4 > 3 \Leftrightarrow (4,3) \in\, > \\
(b,c) \in R \Leftrightarrow bRc  & \Leftrightarrow f(b) > f(c) \Leftrightarrow 3 > 2 \Leftrightarrow (3,2) \in\, > \\
(a,c) \in R \Leftrightarrow aRc  & \Leftrightarrow f(a) > f(c) \Leftrightarrow 4 > 2 \Leftrightarrow (4,2) \in\, > \\
(a,d) \in R \Leftrightarrow aRd  & \Leftrightarrow f(a) > f(d) \Leftrightarrow 4 > 1 \Leftrightarrow (4,1) \in\, > \\
(b,d) \in R \Leftrightarrow bRd  & \Leftrightarrow f(b) > f(d) \Leftrightarrow 3 > 1 \Leftrightarrow (3,1) \in\, > \\
(c,d) \in R \Leftrightarrow cRd  & \Leftrightarrow f(c) > f(d) \Leftrightarrow 2 > 1 \Leftrightarrow (2,1) \in\, > \\
\end{split}
\end{align}
Für die hier definierten Relationssysteme $\mathcal{M}$ und $\mathcal{M}$
existiert also ein Homomorphismus. Es ist dementsprechend leicht einzusehen, dass auch
\begin{equation}
g : M \to \mathbb{R}, m \mapsto g(m) \mbox{ mit }
g(a) := 10, g(b) := 4, g(c) := 2, g(d) := 0
\end{equation}
ein Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$ ist. Für die hier
definierten Relationssysteme $\mathcal{M}$ und $\mathcal{M}$ existiert also sogar
mehr als nur ein Homomorphismus, der oben definierte Homomorphismus $f$ ist also
nicht eindeutig.
:::

Die Nicht-Eindeutigkeit von Homomorphismen induziert den Begriff der *Skala*.

:::{#def-skala}
## Skala

$\mathcal{M}$ sei ein qualitatives Relationssystem mit Menge $M$, $\mathcal{N}$ sei
ein numerisches Relationssystem und
\begin{equation}
\mathcal{H}(\mathcal{M}, \mathcal{N}) :=
\lbrace h : M \to \mathbb{R} | h \mbox{ ist ein Homomorphismus von }
\mathcal{M} \mbox{ nach } \mathcal{N} \rbrace
\end{equation}
sei die Menge aller Homomorphismen von $\mathcal{M}$ nach  $\mathcal{N}$. Dann
heißt für eine Funktion $f : M \to \mathbb{R}$ das Tripel $(\mathcal{M}, \mathcal{N},f)$
*Skala*, wenn $f\in \mathcal{H}(\mathcal{M},\mathcal{N})$.
:::

Die hier gewählte Definition entspricht der Definition der *numerischen Skala* in @roberts1984.

Wenn gilt dass
\begin{equation}
\mathcal{H}(\mathcal{M}, \mathcal{N}) = \emptyset,
\end{equation}
dann existiert  kein  Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$. Wenn gilt dass
\begin{equation}
|\mathcal{H}(\mathcal{M}, \mathcal{N})| > 1,
\end{equation}
dann existiert mehr als ein Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$.

Der Begriff *Skala* wird manchmal auch nur nur für einen Homomorphimus verwendet.
In der Psychologie wird der Begriff *Skala* auch oft ohne Bezug zur Repräsentationstheorie
als Synonym für *Fragebogen* verwendet.


## Repräsentation, Eindeutigkeit und Bedeutsamkeit {#sec-repräsentation-eindeutigkeit-bedeutsamkeit}

:::{#def-repräsentationsproblem-der-repräsentationstheorie}

## Repräsentationsproblem der Repräsentationstheorie
Gegeben seien ein qualitatives Relationssystem $\mathcal{M}$ und ein numerisches
Relationssystem $\mathcal{N}$. Dann besteht das *Repräsentationsproblem der Repräsentationstheorie*
darin, notwendige und hinreichende Eigenschaften von $\mathcal{M}$ für die Existenz eines
Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$ anzugeben.
:::

Für ein gegebenes numerisches Relationssystem $\mathcal{N}$ fragt man in der
Repräsentationstheorie also, wie ein  qualitatives Relationssystem $\mathcal{M}$
beschaffen sein muss, damit ein Homomorphismus  von $\mathcal{M}$ nach $\mathcal{N}$ existiert.
Repräsentationstheoretische Theoreme, die besagen, dass bestimmte Eigenschaften von $\mathcal{M}$
für die Existenz eines Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$ notwendig und hinreichend sind,
werden *Repräsentationstheoreme* genannt. Im Idealfall ist der Beweis eines
Repräsentationstheorems *konstruktiv*, d.h., neben dem Beweis der bloßen Existenz
des Homomorphismus gibt der Beweis auch die funktionale Form des Homomorphismus
an und definiert damit den Repräsentationsvorgang. Die notwendigen und hinreichenden
Eigenschaften von $\mathcal{M}$ für die Existenz eines  Homomorphismus von $\mathcal{M}$
nach $\mathcal{N}$ werden traditionell auch *Axiome* von $\mathcal{M}$. In
@sec-ordinalrepräsentation, @sec-extensivrepräsentation, und @sec-additiv-konjunktrepräsentation
betrachten wir Repräsentationstheorem für spezifische qualitative Relationssysteme.

Wir erinnern in diesem Zusammenhang an die Bedeutung der Begriffe *notwendiger
und hinreichender Bedingungen*. Eigenschaften $B$ sind eine *notwendige Bedingung*
für eine Aussage $K$, wenn sie zwingend erfüllt sein müssen, wenn $K$ erfüllt ist.
Man schreibt dafür $K \Rightarrow B$, "aus $K$ folgt $B$". Gemahlene Bohnen sind
eine notwendige Bedingung beim Kochen von Kaffee, d.h. wenn Kaffee gekocht wird,
existieren gemahlene Bohnen. Hinreichend für das Kochen von Kaffee sind gemahlene
Bohnen aber nicht, es braucht ja z.B. auch noch Wasser und Hitze. Bei der Frage
nach Nullstellen einer Funktion $f : \mathbb{R} \to \mathbb{R}$ ist eine notwendige
Bedingung für das Vorliegen einer Extremstelle in $x$, dass $f'(x) = 0$. Wenn also
$f$ an der Stelle $x$ eine Extremstelle hat, dann gilt $f'(x) = 0$. Hinreichend
für eine Extremstelle von $f$ in $x$ ist $f'(x) = 0$ allerdings nicht, denn es
gilt auch $f'(x) = 0$ in Sattelpunkten. Im Kontext der Repräsentationstheorie
sind *notwendige Eigenschaften von $\mathcal{M}$* solche Eigenschaften von
$\mathcal{M}$, die aus der Existenz eines Homomorphismus von $\mathcal{M}$ nach
$\mathcal{N}$ folgen.

Eigenschaften $B$ sind eine *hinreichende Bedingung* für eine Aussage $K$, wenn aus
$B$ zwingend folgt, dass $K$ erfüllt ist. Man schreibt dafür $B \Rightarrow K$,
"aus $B$ folgt $K$". Nassfutter zu fressen ist eine hinreichende Bedingung für
die Sättigung des Katers, d.h. wenn der Kater Nassfutter gefressen hat, ist er satt.
Allerdings ist Nassfutter zu fressen keine notwendige Bedingung für die Sättigung des Katers,
denn auch wenn der Kater Trockenfutter gefressen hat, ist er satt. Bei der Frage nach
Nullstellen einer Funktion $f : \mathbb{R} \to \mathbb{R}$ sind hinreichende Bedingungen
für das Vorliegen einer Extremstelle in $x$, dass $f'(x) = 0$ und $f''(x) \neq 0$. Aus
$f'(x) = 0$ und $f''(x) \neq 0$ folgt zwingend, dass $f$ in $x$ eine Extremstelle hat.
Im Kontext der Repräsentationstheorie sind *hinreichende Eigenschaften von $\mathcal{M}$*
solche Eigenschaften von $\mathcal{M}$, aus denen die Existenz eines Homomorphismus
von $\mathcal{M}$ nach $\mathcal{N}$ folgt.

Hinreichende und notwendige Eigenschaften $B$ von $\mathcal{M}$ für die Existenz
$K$ eines Homomorphismus sind also die kleinstmögliche Menge an Eigenschaften
von $\mathcal{M}$, die für die Existenz eines Homomorphismus genügen, so dass gilt
\begin{equation}
B \Rightarrow K \mbox{ und } K \Rightarrow B \mbox{, also } B \Leftrightarrow K.
\end{equation}


### Vorgehensweisen repräsentationstheoretischer Überlegungen {-}


$\Rightarrow$ Vom qualitativen Relationssystem zum Homomorphismus und numerischen Relationssystem

Im Anwendungskontext stellt man sich vor, dass ein bestimmter Gegenstandsbereich
als qualitatives Relationssystem mit bestimmten Eigenschaften beschrieben werden kann.
Letztlich sind die Eigenschaften empirisch zu überprüfen, dafür bietet die klassische
Messtheorie aber keine Methodik. Man legt also ein in gewisser Weise idealisiertes
qualitatives Relationssystem zugrunde und fragt dann, ob es einen Homomorphismus
in ein numerisches Relationssystem, z.B. $(\mathbb{R},>)$ oder $(\mathbb{R},>,+)$ geben kann.
Insgesamt hat dieser Ansatz sicherlich einen hohen intuitiven Appeal.

$\Rightarrow$  Vom numerischen Relationssystem und Homomorphismus zum qualitativen Relationssystem

Im Theoriekontext fragt man dagegen oft, wenn man ein bestimmtes numerisches Relationssystem,
z.B. $(\mathbb{R},>)$ oder $(\mathbb{R},>,+)$ und die Existenz eines Homomorphismus zugrundelegt,
welche Eigenschaften dann ein qualitatives Relationssystem mindestens genügen muss.
Dementsprechend geht es einerseits darum, numerische Relationen wie $>$ oder $\ge$ oder Operationen
wie $+$ auf den reellen Zahlen mithilfe qualitativer Relationsbegriffe zu beschreiben.
Dies motiviert dann z.B. Beispiel die Charakterisierung von (qualitativen) Ordnungsrelationen.
Zum anderen versucht man, nur eben jene Eigenschaften von qualitativen Relationssystemen zu fordern,
die für den Nachweis der Existenz eines Homomorphismus in ein bestimmtes numerisches Relationssystem
oder die so induzierte Skalenart gerade nötig sind. Der intuitive Appeal dieser
Herangehensweise ist sicherlich geringer, die erreichbare formale Strenge und Einfachheit
jedoch leichter gewährleistet als im obigem Ansatz. Die Inhalte der Einheiten zu Ordinalmessungen,
Extensivmessungen und Differenzmessungen sollten auch vor dem Hintergrund dieses
zweiten Ansatzes rezipiert werden.


:::{#def-eindeutigkeitsproblem-der-repräsentationstheorie}
## Eindeutigkeitsproblem der Repräsentationstheorie

Gegeben seien ein qualitatives Relationssystem $\mathcal{M}$, ein numerisches
Relationssystem $\mathcal{N}$ und ein Homomorphismus $f$ von $\mathcal{M}$ nach $\mathcal{N}$.
Dann besteht das *Eindeutigkeitsproblem der Repräsentationstheorie* darin, zu bestimmen,
ob dieser Homomorphismus der einzige Homomorphismus ist, der zwischen $\mathcal{M}$
und $\mathcal{N}$ existiert.
:::

Die Antworten auf Eindeutigkeitsprobleme legen die sogenannten *Skalenarten* fest
und bilden die Grundlage für den repräsentationstheoretischen Begriff der *Bedeutsamkeit*.
In @sec-skalenarten befassen wir uns mit dem Eindeutigkeitsproblem der
Repräsentationstheorie.


:::{#def-bedeutsamkeit}

## Bedeutsamkeit
$\mathcal{M}$ sei ein qualitatives Relationssystem und $\mathcal{N}$ sei ein
numerisches Relationssystem. Dann heißt eine Aussage bezüglich $\mathcal{M}$ und
$\mathcal{N}$ *bedeutsam}, wenn ihr Wahrheitsgehalt unverändert bleibt, wenn eine
beliebige Skala $(\mathcal{M},\mathcal{N},f)$ durch eine andere Skala
$(\mathcal{M}, \mathcal{N},g)$ ersetzt wird.
:::

Mithilfe der Theorie der Skalenarten kann der Begriff der Bedeutsamkeit klarer mithilfe
des Begriffs der *zulässigen Skalentransformationen* formuliert werden. Der
Begriff der Bedeutsamkeit wird manchmal herangezogen, um zu argumentieren, dass
bestimmte   Datenanalysen bei bestimmten Daten "erlaubt" und andere "nicht erlaubt
sind". Wir werden diese Aspekte in @sec-bedeutsamkeit näher beleuchten und kritisch diskutieren.
 Zum Einstieg geben wir im Folgenden zwei Beispiele für intuitiv bedeutsame und nicht bedeutsame Aussagen.


:::{#exm-bedeutsame-aussagen-beim-messen-der-masse-von-objekten}
## Bedeutsame Aussagen beim Messen der Masse von Objekten

Die Aussage "Objekt $m$ ist doppelt so schwer wie Objekt $n$" ist intuitiv bedeutsam,
da unabhängig davon, ob in Gramm, Kilogramm (1 kg = 1000 g), oder
angloamerikanischen Pfund (1 lb = 0.453 kg) gemessen wird, die Aussage wahr bleibt.
Seien zum Beispiel den Objekten $m$ und $n$ durch
die Gramm-Skala die Gewichte 400 g und 200 g zugeordnet. Dann gilt
\begin{equation}
\frac{\mbox{Gewicht von } m}{\mbox{Gewicht von } n} = 2
\rightarrow
\frac{400 \mbox{ g}}{200 \mbox{ g}} = 2
\rightarrow
\frac{0.4 \mbox{ kg}}{0.2 \mbox{ kg}} = 2
\rightarrow
\frac{0.88 \mbox{ lb}}{0.44 \mbox{ lb}} = 2.
\end{equation}
:::

:::{#exm-nicht-bedeutsame-aussagen-beim-messen-der-temperatur-von-objekten}
## Nicht-bedeutsame Aussagen beim Messen der Temperatur von Objekten

Die Aussage "Objekt $m$ ist doppelt so warm wie Objekt $n$" ist intuitiv nicht bedeutsam,
da abhängig davon, ob in Grad Celsius (T$_{\mbox{\tiny C}}$), Fahrenheit
(T$_{\mbox{\tiny F}}$ = $\frac{9}{5}$ T$_{\mbox{\tiny C}}$ + 32), oder Kelvin
(T$_{\mbox{\tiny K}}$ = T$_{\mbox{\tiny C}}$ + 275.15)
gemessen wird, die Aussage falsch wird. Seien zum Beispiel den Objekten $m$ und
 $n$ durch  die Celsius-Skala die Temperaturen 40°C und 20°C zugeordnet. Dann gilt
\begin{equation}
\frac{\mbox{Temperatur von } m}{\mbox{Temperatur von } n} = 2
\rightarrow
\frac{40^{\circ} \mbox{C}}{20^{\circ} \mbox{C}} = 2
\rightarrow
\frac{104^{\circ} \mbox{ F}}{68^{\circ} \mbox{ F}} = 1.52 \neq 2
\rightarrow
\frac{313.15\mbox{ K}}{293.15\mbox{ K}} = 1.06 \neq 2.
\end{equation}
Das Messen von Temperatur von Objekten wird in der Folge unser Arbeitsbeispiel
für den Begriff der *Differenzmessung* und dem zugehörigen Begriff der
*Intervallskala* sein.
:::

## Reguläre Repräsentationen und Skalenarten {#sec-reguläre-repräsentationen-und-skalenarten}

Basierend auf den Beobachtungen zur Bedeutsamkeit von Aussagen bei Gewichts- und
Temperaturmessungen besteht spätestens seit @stevens1946 ein Verlangen, zu
klassifizieren welche Art von Aussagen und Operationen im Zahlenraum bei welcher
Art von Repräsentationsproblem angemessen ist. Traditionell führt dies auf die Begriffe der
Ordinal-, Verhältnis, und Intervallskala, wie in Tabelle 1 in @stevens1946 skizziert.
Verschiedenen Skalen hier sind im Sinne des Eindeutigkeitsproblems der Repräsentationstheorie
eindeutig bis auf eine bestimmte Art von Funktionstransformation. Es ergibt sich also die
Frage, was der Begriff der *Skalenart* bezeichnet und vor dem Hintegrund der Repräsentationstheorie
bedeutet. Die folgenden Ausführungen dazu basieren auf @roberts1984, Kapitel 2 und insbesondere
@roberts1976. Die Begriffe der Ordinal-, Verhältnis-, und Intervallskala stehen
in Korrespondenz zu den Begriffen der Ordinal-, Extensiv-, und Differenzmessung,
respektive, die wir in nachfolgenden Einheiten diskutieren. Den Begriff der
Nominalskala sparen wir aus.

### Reguläre Repräsentationen {-}


:::{#def-zulässige-transformationen-einer-skala}

## Zulässige Transformationen einer Skala
Gegeben sei eine Skala $(\mathcal{M},\mathcal{N},f)$ mit zugrundeliegenden Mengen $M$
und $\mathbb{R}$. Sei weiterhin $\phi$ eine Funktion der Form $\phi : f(M) \to \mathbb{R}$,
wobei $f(M) := \{f(m)|m\in M\}\subseteq \mathbb{R}$ die Bildmenge von $M$ unter $f$ bezeichnet.
Sei schließlich $\phi \circ f : M \to \mathbb{R}$ die Verkettung von $\phi$ und $f$.
Dann heißt $\phi$ eine *zulässige Transformation von $(\mathcal{M},\mathcal{N},f)$*,
wenn $\phi \circ f$ ein Homomorphismus bezüglich $\mathcal{M}$ und $\mathcal{N}$ ist.
:::

Zur Konstruktion von $f, \phi$ und $\phi\circ f$, siehe @fig-zulässige-skalentransformationen

![Konstruktion von $f, \phi$ und $\phi\circ f$.](./_figures/905-zulässige-skalentransformationen.pdf){#fig-zulässige-skalentransformationen fig-align="center" width=60%}

:::{#exm-zulässige-und-nicht-zulässige-transformationen}

## Zulässige und nicht zulässige Transformationen

Es seien $\mathcal{M} := (\mathbb{N},>)$, $\mathcal{N} := (\mathbb{R},>)$ und
$f : \mathbb{N} \to \mathbb{R}, n \mapsto f(n) := 2n$. Dann ist
$f \in \mathcal{H}(\mathcal{M},\mathcal{N})$ und $(\mathcal{M},\mathcal{N},f)$ eine Skala,
weil für alle $n,m\in \mathbb{N}$ gilt, dass
\begin{equation}
n > m \Leftrightarrow 2n > 2m \Leftrightarrow f(n) > f(m).
\end{equation}

1. Es sei nun $\phi : f(\mathbb{N}) \to \mathbb{R}, x \mapsto \phi(x) := x + 5$.
Dann ist $\phi$ eine zulässige Transformation von $(\mathcal{M},\mathcal{N},f)$,
denn auch
\begin{equation}
\phi \circ f : \mathbb{N} \to \mathbb{R}, n \mapsto (\phi \circ f)(n) := \phi(f(n)) = 2n + 5
\end{equation}
ist ein Homomorphismus, weil für alle $n,m\in \mathbb{N}$ auch gilt, dass
\begin{equation}
n > m \Leftrightarrow 2n + 5 > 2m + 5 \Leftrightarrow (\phi \circ f)(n) > (\phi \circ f)(m).
\end{equation}
1. Es sei nun $\phi : f(\mathbb{N}) \to \mathbb{R}, x \mapsto \phi(x) := -x$.
Dann ist $\phi$ keine zulässige Transformation von $(\mathcal{M},\mathcal{N},f)$,
denn
\begin{equation}
\phi \circ f : \mathbb{N} \to \mathbb{R}, n \mapsto (\phi \circ f)(n) := \phi(f(n)) = -2n
\end{equation}
ist kein Homomorphismus, weil für alle $n,m\in \mathbb{N}$ gilt
\begin{equation}
n > m \Leftrightarrow -2n < -2m \,\,\not\Leftrightarrow\,\, (\phi \circ f)(n) > (\phi \circ f)(m).
\end{equation}
:::

:::{#def-reguläre-skala-und-reguläre-repräsentation}

## Reguläre Skala und reguläre Repräsentation
$\mathcal{M}$ sei ein qualitatives Relationssystem, $\mathcal{N}$ sei ein numerisches
Relationssystem und $\mathcal{H}(\mathcal{M},\mathcal{N})$ sei die Menge der Homomorphismen
von $\mathcal{M}$ nach $\mathcal{N}$. Eine Skala $(\mathcal{M},\mathcal{N},f)$ heißt
*regulär*, wenn zu jedem $g \in \mathcal{H}(\mathcal{M},\mathcal{N})$ eine
Funktion $\phi$ gefunden werden kann, so dass $g = \phi \circ f$ gilt. Wenn jede
Skala $(\mathcal{M},\mathcal{N}, f)$ regulär ist, sagt man, dass die Repräsentation
$\mathcal{M}\to \mathcal{N}$ *regulär* ist.
:::

Für $(\mathcal{M},\mathcal{N},f)$ und $g \in \mathcal{H}(\mathcal{M},\mathcal{N})$
ist ein $\phi$ mit $g = \phi \circ f$ natürlich eine zulässige Transformation von
$f$. Bei zulässigen Transformationen fragt man, ob für $f$ und $\phi$ gilt, dass
$g = \phi \circ f$ ein Homomorphismus ist. Bei regulären Skalen dagegen geht man von
$f,g \in \mathcal{H}(\mathcal{M},\mathcal{N})$ aus und fragt, ob es ein $\phi$ mit
$g = \phi \circ f$ gibt. Reguläre Repräsentationen vereinfachen die Beantwortung
des Eindeutigkeitsproblems und induzieren den Begriff der *Skalenart*. Die
Regularität einer Repräsentation liegt in der speziellen Form von $\mathcal{M}$
nach $\mathcal{N}$ begründet.

:::{#exm-reguläre-repräsentation}

## Reguläre Repräsentation
Es seien $\mathcal{M} := (\mathbb{N},>)$, $\mathcal{N} := (\mathbb{R},>)$ und
\begin{equation}
f : \mathbb{N} \to \mathbb{R}, n \mapsto f(n) := 2n
\end{equation}
sowie
\begin{equation}
g : \mathbb{N} \to \mathbb{R}, n \mapsto g(n) := 7n
\end{equation}
Dann sind $f,g\in \mathcal{H}(\mathcal{M}, \mathcal{N})$ und $(\mathcal{M},\mathcal{N},f)$
und $(\mathcal{M},\mathcal{N},g)$ sind Skalen. In diesem Fall ergibt sich
\begin{equation}
g = \phi \circ f \mbox{ für }\phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \frac{7}{2}x,
\end{equation}
denn
\begin{equation}
(\phi \circ f)(n) = \phi(2n) = \frac{7}{2}\cdot 2n = 7n = g(n).
\end{equation}
Per Konstruktion ist $g = \phi\circ f\in \mathcal{H}(\mathcal{M}, \mathcal{N})$
und $\phi$ damit eine zulässige Transformation von $f$.
:::

:::{#exm-nicht-reguläre-repräsentation}

## Nicht-reguläre Repräsentation
Es sei $\mathcal{M} := (M,R)$ mit $M := \{r,s,t\}$ und $R := \{(r,s),(r,t)\}$.
Weiterhin sei $\mathcal{N} := (\mathbb{R},>_{1})$, wobei
\begin{equation}
x >_{1} y \Leftrightarrow x > y + 1
\end{equation}
für alle $x,y\in \mathbb{R}$. Dann ist
\begin{equation}
f : M \to \mathbb{R} \mbox{ mit } f(r) := 2, f(s) := 0, f(t) := 0
\end{equation}
ein Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$, weil
\begin{align}
\begin{split}
(r,s) \in R & \Leftrightarrow 2 > 0 + 1 \Leftrightarrow 2 >_1 0 \Leftrightarrow f(r) >_{1} f(s) \\
(r,t) \in R & \Leftrightarrow 2 > 0 + 1 \Leftrightarrow 2 >_1 0 \Leftrightarrow f(r) >_{1} f(t).
\end{split}
\end{align}
Weiterhin ist
\begin{equation}
g : M \to \mathbb{R} \mbox{ mit } g(r) := 2.0, g(s) := 0.1, g(t) := 0.0
\end{equation}
ein Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$, weil
\begin{align}
\begin{split}
(r,s) \in R & \Leftrightarrow 2.0 > 0.1 + 1 \Leftrightarrow 2 >_1 0.1 \Leftrightarrow g(r) >_{1} g(s) \\
(r,t) \in R & \Leftrightarrow 2.0 > 0.0 + 1 \Leftrightarrow 2 >_1 0.0 \Leftrightarrow g(r) >_{1} g(t).
\end{split}
\end{align}
Allerdings existiert keine Funktion der Form $\phi:f(M) \to \mathbb{R}$ mit $g = \phi \circ f$,
denn für ein solches $\phi$ müsste
\begin{equation}
g(s) = (\phi \circ f)(s) = (\phi \circ f)(t) = g(t)
\end{equation}
gelten, weil $f(s) = f(t)$ und $\phi$ als Funktion jedem $x \in f(M)$ genau ein
$\phi(x) \in \mathbb{R}$ zuordnet. Es gilt aber nach Definition $g(s) = 0.1 \neq 0.0 = g(t)$.

:::


:::{#thm-charakterisierung-regulärer-skalen}
## Charakterisierung regulärer Skalen

Eine Skala $(\mathcal{M}, \mathcal{N},f)$ ist dann und nur dann regulär, wenn
für alle $g \in \mathcal{H}(\mathcal{M}, \mathcal{N})$ und $m,n \in M$ gilt, dass
aus $f(m) = f(n)$ folgt, dass $g(m) = g(n)$.
:::

:::{.proof}

*$(\mathcal{M}, \mathcal{N},f)$ ist regulär $\Rightarrow$ Aus $f(m) = f(n)$ folgt, dass $g(m) = g(n)$*

Sei zunächst $(\mathcal{M}, \mathcal{N},f)$  ein reguläre Skala. Dann existiert
für jede beliebige Skala $(\mathcal{M}, \mathcal{N},g)$, also insbesondere für
jedes $g \in \mathcal{H}(\mathcal{M}, \mathcal{N})$ eine Funktion der Form
$\phi : f(M) \to \mathbb{R}$, so dass $g = \phi \circ f$. Es gilt also
\begin{equation}
f(m) = f(n) \Rightarrow (\phi \circ f)(m) = (\phi \circ f)(n) \Leftrightarrow g(m) = g(n),
\end{equation}
da $\phi$ als Funktion jedem $x \in f(M)$ genau ein (und nicht etwa mehrere)
$\phi(x)$ zuordnet.

*Aus $f(m) = f(n)$ folgt, dass $g(m) = g(n) \Rightarrow (\mathcal{M}, \mathcal{N},f)$ ist regulär."*

Sei umgekehrt ein $g \in \mathcal{H}(\mathcal{M}, \mathcal{N})$ mit $g(m) = g(n)$ und definiere
\begin{equation}
\phi \circ f : f(M) \to \mathbb{R}, m \mapsto (\phi \circ f)(m) := g(m).
\end{equation}
Dann gilt, weil aus $f(m) = f(n)$ folgt, dass $g(m) = g(n)$ und damit
$(\phi \circ f)(m) := (\phi \circ f)(n)$ ist, dass $\phi$ eine Funktion ist, also jedem $x \in f(M)$
genau ein (und nicht etwa mehrere) $\phi(x)$ zuordnet. Weiterhin ist $g = \phi \circ f$.
Es existiert also für eine beliebige Skala $(\mathcal{M}, \mathcal{N},g)$ eine
Funktion der Form $\phi \circ f : f(M) \to \mathbb{R}$ mit $g = \phi \circ f$, und
damit ist die Skala $(\mathcal{M}, \mathcal{N},f)$ regulär.
:::

:::{#thm-isomorphismen-und-regularität}

## Isomorphismen und Regularität
Wenn $f$ ein Isomorphismus ist, dann ist $(\mathcal{M}, \mathcal{N},f)$ regulär.
:::

:::{.proof}

Wenn $f$ ein Isomorphismus ist, dann ist $f$ ein injektiver Homomorphismus von
$\mathcal{M}$ nach $\mathcal{N}$. Insbesondere gilt für $f$ also, dass aus
$f(m) = f(n)$ folgt, dass $m = n$ ist. Sei $g$ ein weiterer Homomorphismus von
$\mathcal{M}$ nach $\mathcal{N}$. Dann ist $g$ insbesondere eine Abbildung von
$M$ nach $\mathbb{R}$ und ordnet jedem $m \in M$ genau ein $g(m) \in \mathbb{R}$ zu.
Also folgt aus $m = n$, dass $g(m) = g(n)$. Also impliziert für einen Isomorphismus
und alle $m,n \in M$ $f(m) = f(n)$ immer, dass für einen weiteren Homomorphismus
$g$ von $\mathcal{M}$ nach $\mathcal{N}$ gilt, dass $g(m) = g(n)$ und dies ist
gerade die Bedingung für die Regularität von $(\mathcal{M}, \mathcal{N},f)$
nach dem Theorem zur Charakterisierung regulärer Skalen.

### Skalenarten {-}

:::{#def-skalenarten}

## Skalenarten
Gegeben sei eine Skala $(\mathcal{M},\mathcal{N},f)$ mit zugrundeliegenden Mengen
$M$ und $\mathbb{R}$ und Homomorphismus $f$. Weiterhin sei
\begin{equation}
\Phi := \{\phi| \phi : f(M) \to \mathbb{R} \}
\end{equation}
die Menge der zulässigen Transformationen von $(\mathcal{M},\mathcal{N},f)$. Die *Skalenart*
von $(\mathcal{M},\mathcal{N},f)$  ist dann durch $\Phi$ bestimmt. Insbesondere gelten:

1. Wenn die Menge der zulässigen Transformationen der Skala die Menge der *monoton steigenden Funktionen*
\begin{equation}
\Phi := \{\phi| \phi : f(M) \to \mathbb{R}, x \mapsto \phi(x)  \mbox{ mit } x > y \Leftrightarrow \phi(x) > \phi(y) \mbox{ für alle } x,y \in f(M)\}
\end{equation}
ist, dann heißt $(\mathcal{M},\mathcal{N},f)$ *Ordinalskala*.
1. Wenn die Menge der zulässigen Transformationen der Skala  die Menge der
*positiv linear-affinen Funktionen*
\begin{equation}
\Phi := \{\phi| \phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \alpha x + \beta  \mbox{ mit } \alpha > 0\}
\end{equation}
ist, dann heißt $(\mathcal{M},\mathcal{N},f)$ *Intervallskala*.
1. Wenn die Menge der zulässigen Transformationen der Skala die Menge der
*Ähnlichkeitstransformationen*
\begin{equation}
\Phi := \{\phi| \phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \alpha x \mbox{ mit } \alpha > 0\}
\end{equation}
ist, dann heißt $(\mathcal{M},\mathcal{N},f)$ *Verhältnisskala*.

:::

Wir werden im weiteren Verlauf sehen, dass Ordinalstrukturen Ordinalskalen induzieren,
Extensivstrukturen Verhältnisskalen induzieren und dass ...

:::{#exm-nachweis-der-ordinalität-einer-skala}
## Nachweis der Ordinalität einer Skala

Gegeben sei das qualitative Relationssystem $\mathcal{M} := (M,R)$ mit
\begin{equation}
M := \{r,s,t\} \mbox{ und } R := \{(r,s),(s,t),(r,t)\}
\end{equation}
$\mathcal{M}$ ist homomorph zu dem numerischen Relationssystem $\mathcal{N} := (\mathbb{R},>)$,
denn mit
\begin{equation}
f : M \to \mathbb{R} \mbox{ mit } f(r) := 2, f(s) := 1, f(t) = 0
\end{equation}
existiert ein Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$ und $\mathcal{H}(\mathcal{M},\mathcal{N})\neq \emptyset$:
\begin{align}
\begin{split}
(r,s) \in R & \Leftrightarrow 2 > 1 \Leftrightarrow f(r) > f(s) \Leftrightarrow (f(r), f(s)) \in\, > \\
(s,t) \in R & \Leftrightarrow 1 > 0 \Leftrightarrow f(s) > f(t) \Leftrightarrow (f(s), f(t)) \in\, > \\
(r,t) \in R & \Leftrightarrow 2 > 0 \Leftrightarrow f(r) > f(t) \Leftrightarrow (f(r), f(t)) \in\, > \\
\end{split}
\end{align}
Da $f$ auch injektiv ist, ist die Repräsentation regulär. Weiterhin ist $\phi$
eine zulässige Transformation von $f$ dann und nur dann, wenn $g := \phi \circ f$
wiederum ein Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$ ist, wenn
also gilt
\begin{align}
\begin{split}
g(r) > g(s) & \Leftrightarrow (\phi \circ f)(r) > (\phi \circ f)(s) \Leftrightarrow \phi(2) > \phi(1) \\
g(s) > g(t) & \Leftrightarrow (\phi \circ f)(s) > (\phi \circ f)(t) \Leftrightarrow \phi(1) > \phi(0) \\
g(r) > g(t) & \Leftrightarrow (\phi \circ f)(r) > (\phi \circ f)(t) \Leftrightarrow \phi(2) > \phi(0) \\
\end{split}
\end{align}
Dann gilt für $\phi$ mit $f(M) := \{0,1,2\}$ aber, dass
\begin{equation}
\phi : f(M) \to \mathbb{R}, x \mapsto \phi(x)  \mbox{ mit } x > y \Leftrightarrow \phi(x) > \phi(y) \mbox{ für alle } x,y \in f(M)
\end{equation}
und $\phi$ ist dann und nur dann eine zulässige Transformation von $f$, wenn $\phi$ eine monoton steigende Funktion ist. Damit
ist $(\mathcal{M}, \mathcal{N},f)$ eine Ordinalskala.
:::

Folgendes Theorem zeigt, dass bei regulären Repräsentationen alle Skalen von der
gleichen Skalenart sind.

:::{#thm-skalenart}

## Skalenart
Wenn eine Repräsentation $\mathcal{M} \to  \mathcal{N}$ regulär ist und wenn
$f$ und $g$ Homormorphismen von $\mathcal{M}$ nach $\mathcal{N}$ sind, dann ist
$f$ eine Ordinalskala, Verhältnisskala, oder Intervallskala genau dann und nur
dann, wenn $g$ eine Ordinalskala, Verhältnisskala, oder Intervallskala, respektive, ist.
:::

:::{.proof}

Wir beweisen das Theorem nur für den Fall einer Ordinalskala. Wir zeigen dabei
auch lediglich, dass aus der Regularität von $\mathcal{M} \to  \mathcal{N}$ und der Tatsache,
dass $f$ eine Ordinalskala ist, folgt, dass ein weiterer Homomorphismus $g$ von
$\mathcal{M}$ nach $\mathcal{N}$ ebenfalls eine Ordinalskala ist.

$f$ sei eine Ordinalskala. Wir halten zunächst fest, dass weil die Repräsentation
$\mathcal{M} \to  \mathcal{N}$ regulär ist, es ein $\phi : f(M) \to \mathbb{R}$ mit
$g = \phi \circ f$ gibt. Weiterhin gilt, dass weil $g$ ein Homomorphismus ist, ein solches
$\phi$ eine zulässige Transformation von $f$ ist. Schließlich gilt, dass weil
$f$ eine Ordinalskala ist und damit per Definition alle zulässigen Transformationen
von $f$ monoton steigende Funktionen sind, ein solches $\phi$ eine monoton steigende Funktion ist.

Der restliche Beweis erfolgt durch Beweis folgender Implikationen:

1. $\phi' : g(M) \to \mathbb{R}$ ist monoton steigend $\quad\,\,\Rightarrow$ $\phi'$ ist eine zulässige Transformation von $g$.
1. $\phi'$ ist eine zulässige Transformation von $g \Rightarrow \phi' : g(M) \to \mathbb{R}$ ist monoton steigend.

Also gilt
\begin{equation}
\phi' \mbox{ ist eine zulässige Transformation von } g \Leftrightarrow \phi' : g(M) \to \mathbb{R} \mbox{ ist monoton steigend}.
\end{equation}
Die Klasse der zulässigen Transformation von $g$ ist damit also die Klasse der
monoton steigenden Funktionen und $g$ ist damit eine Ordinalskala.

*$\phi' : g(M) \to \mathbb{R}$ ist monoton steigend $\Rightarrow$ $\phi'$ ist eine zulässige Transformation von $g$*

Die Implikation gilt, weil mit obigem $\phi$ gilt, dass
\begin{equation}
\phi' \circ g = \phi' \circ (\phi \circ f) = (\phi' \circ \phi) \circ f.
\end{equation}
Weil $\phi'$ und $\phi$ monoton steigende Funktionen sind, ist $\phi' \circ \phi$ auch monoton steigend
und damit eine zulässige Transformation von $f$. Damit ist wiederum $\phi'\circ g$ ein Homomorphismus
von $\mathcal{M}$ nach $\mathcal{N}$ und $\phi'$ daher eine zulässige Transformation
von $g$.

*$\phi'$ ist eine zulässige Transformation von $g \Rightarrow \phi' : g(M) \to \mathbb{R}$ ist monoton steigend*

Die Implikation gilt, weil mit obigem $\phi$ gilt, dass
\begin{equation}
\phi' \circ g = \phi' \circ (\phi \circ f) = (\phi' \circ \phi) \circ f
\end{equation}
ein Homorphismus von $\mathcal{M}$ nach $\mathcal{N}$ ist und $\phi' \circ \phi$
somit eine zulässige Transformation $f$. Damit muss $\phi' \circ \phi$ aber eine
monoton steigende Funktion sein und deshalb auch $\phi'$.
:::

Man unterscheidet weiterhin mindestens die in @tbl-weitere-skalenarten aufgeführten
Skalenarten, wobei jede dieser Skalenarten ist durch die spezielle Form von
$\mathcal{M}$ und $\mathcal{N}$ determiniert ist

\footnotesize

: Weitere Skalenarten {#tbl-weitere-skalenarten}

|Name                           | Menge der zulässigen Transformationen                                                                  |
|:------------------------------|:-------------------------------------------------------------------------------------------------------|
|Nominalskala                   | $\Phi := \{\phi| \phi : f(M) \to \mathbb{R} \mbox{ und } \phi \mbox{ ist injektiv}\}$                  |
|Differenzskala                 | $\Phi := \{\phi| \phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := x - \beta, \beta \in \mathbb{R}\}$   |
|Log-Intervallskala             | $\Phi := \{\phi| \phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \alpha x^{\beta}, \alpha,\beta >0\}$ |
|Absolutskala                   | $\Phi := \{\phi| \phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := x\}$                                 |

\normalsize

## Bedeutsame Aussagen und bedeutsame Statistiken {#sec-bedeutsame-aussagen-und-bedeutsame-statistiken}

Die repräsentationstheoretische Bedeutsamkeit fragt nach der Sinnhaftigkeit
von Aussagen zu numerischen Werten und ihrer Manipulation in Abhängigkeit der Skalenart
von $(\mathcal{M},\mathcal{N},f)$. Der Begriff der "Bedeutsamkeit" wird hier
als rein technischer Begriff der Repräsentationstheorie und nicht intuitiv verstanden.

Die Aussagen der Repräsentationstheorie zur Bedeutsamkeit sind vor
dem Hintergrund dieser Theorie sinnvoll und nachvollziehbar, wie wir in dieser
Einheit zeigen wollen. Im größeren Kontext der Datenanalyse und Datenmodellierung
sind sie allerdings extrem umstritten, was letztlich in eine Fundamentalkritik an der
Repräsentationstheorie mündet. @davison1988 bringen die Diskussion zu diesem Thema
auf den Punkt:

*"In his classic work, @stevens1951 outlined his now-famous four levels of
measurement and delineated which statistics were appropriate for the various levels.
Thus began the controversy about the appropriateness of parametric statistics
for ordinal data. On one side is the view that parametric statistics, such as
the $t$ test, are inappropriate for ordinal data (@townsend1984). According to
the opposing view, the level of measurement is irrelevant (@borgatta1980, @gaito1980)."*


### Bedeutsame Aussagen {-}

:::{#def-bedeutsame-aussagen}

$(\mathcal{M},\mathcal{N},f)$ sei eine Skala. Eine Aussage heißt *bedeutsam*
bezüglich $(\mathcal{M},\mathcal{N},f)$, wenn der Wahrheitsgehalt der Aussage
unter allen zulässigen Transformationen von $(\mathcal{M},\mathcal{N},f)$ unverändert bleibt.
:::

Die Definition präzisiert die vorläufige Definition der Bedeutsamkeit (vgl. @def-bedeutsamkeit)
Die hier verwendete Definition der bedeutsamen Aussagen folgt @roberts1984 Kapitel 2.4.
@narens2002 diskutiert eine generalisierte und grundlegendere Theorie der bedeutsamen Aussagen.
Wir betrachten im Folgenden drei Beispiele für bedeutsame Aussagen in Abhängigkeit
der Skalenart. Die gewählten  Beispiele sind dabei prototypisch, aber nicht erschöpfend: es gibt
viele weitere mögliche Aussagen


:::{#thm-bedeutsamkeit-von-größenvergleichen}

## Bedeutsamkeit von Größenvergleichen

$(\mathcal{M},\mathcal{N},f)$ sei eine Skala. Dann ist für $m,n \in M$ die Aussage
\begin{equation}
f(m) > f(n)
\end{equation}
bedeutsam, wenn $f$ eine Ordinalskala, Intervallskala oder eine Verhältnisskala ist.
:::

:::{.proof}

*Bedeutsamkeit bei Ordinalskala*

$f$ sei eine Ordinalskala. Dann ist die Menge $\Phi$ der zulässigen Transformationen
von $f$ die Menge der monoton steigenden Funktionen. Für jede Transformation
von $\phi \circ f$ von $f$ mit $\phi \in \Phi$ gilt also
\begin{equation}
f(m) > f(n) \Leftrightarrow \phi(f(m)) > \phi(f(n)) \Leftrightarrow (\phi \circ f)(m) > (\phi \circ f)(n).
\end{equation}

*Bedeutsamkeit bei Intervallskala*

$f$ sei eine Intervallskala. Dann ist die Menge $\Phi$ der zulässigen Transformationen
von $f$ die Menge der positiv linear-affinen Funktionen. Für jede Transformation
von $\phi \circ f$ von $f$ mit $\phi \in \Phi$ gilt mit $\alpha > 0, \beta \in \mathbb{R}$ $ also
\begin{equation}
f(m) > f(n) \Leftrightarrow \alpha f(m) > \alpha f(n) \Leftrightarrow \alpha f(m) + \beta > \alpha f(n) + \beta \Leftrightarrow (\phi \circ f)(m) > (\phi \circ f)(n).
\end{equation}

*Bedeutsamkeit bei Verhältnisskala*

$f$ sei eine Verhältnisskala. Dann ist die Menge $\Phi$ der zulässigen Transformationen
von $f$ die Menge der Ähnlichkeitstransformationen. Für jede Transformation
von $\phi \circ f$ von $f$ mit $\phi \in \Phi$ gilt mit $\alpha > 0$ also
\begin{equation}
f(m) > f(n) \Leftrightarrow \alpha f(m) >  \alpha f(n) \Leftrightarrow (\phi \circ f)(m) > (\phi \circ f)(n).
\end{equation}
:::

@thm-bedeutsamkeit-von-größenvergleichen kann konkret wie folgt interpretiert werden:
Modelliert man die Messung von Entscheidungsoptionspräferenzen mit einer Ordinalskala,
so bleibt die Aussage "Option $m$ hat einen höheren Präferenzwert als Option $n$"
unabhängig von der speziellen Form von $f$ wahr. Modelliert man das Messen von
Temperaturen mit einer Intervallskala, so bleibt die Aussage "Objekt $m$ hat eine
höhere Temperatur als Objekt $n$" unabhängig von der speziellen Form von $f$
(z.B. $^{\circ}$C oder $^{\circ}$F) wahr. Modelliert man das Messen von Massen
mit einer Verhältnisskala,  so bleibt die Aussage "Objekt $m$ hat eine größere
Masse als Objekt $n$" unabhängig von der speziellen Form von $f$ (z.B. kg oder lb) wahr.


:::{#thm-bedeutsamkeit-von-größenvergleichen-von-differenzen}

## Bedeutsamkeit von Größenvergleichen von Differenzen
$(\mathcal{M},\mathcal{N},f)$ sei eine Skala. Dann ist für $m,n,p,q\in M$ die Aussage
\begin{equation}
f(m) - f(n) > f(p) - f(q)
\end{equation}
im Allgemeinen nicht bedeutsam, wenn $f$ eine Ordinalskala ist und bedeutsam,
wenn $f$ eine Intervallskala oder eine Verhältnisskala ist.
:::

:::{.proof}

*Nicht-Bedeutsamkeit bei Ordinalskala*

$f$ sei eine Ordinalskala. Dann ist die Menge $\Phi$ der zulässigen Transformationen
von $f$ die Menge der monoton steigenden Funktionen. Wir zeigen durch ein Gegenbeispiel,
dass die Aussage nicht unter allen zulässigen Transformationen von $f$ wahr bleibt.
Beispielsweise sei $M := \{m,n,p,q\}$ die Menge des qualitativen Relationssystems
und ein Homomorphismus von $\mathcal{M}$ nach $(\mathbb{R},>)$
definiert durch
\begin{equation}
f : M \to \mathbb{R} \mbox{ mit } f(m) := 10,  f(n) := 6,  f(p) := 4 \mbox{ und }  f(q) := 2
\end{equation}
Sei weiterhin eine monoton steigende Funktion $\phi$ auf $f(M)$ definiert durch
\begin{equation}
\phi : f(M) \to \mathbb{R} \mbox{ mit } \phi(2) := 3,  \phi(4) := 7,  \phi(6) := 9 \mbox{ und }  \phi(10) := 11.
\end{equation}
Dann gilt
\begin{equation}
f(m) - f(n) > f(p) - f(q)
\Leftrightarrow
10 - 6  > 4 - 2
\Leftrightarrow
4 > 2
\end{equation}
aber es gelten auch
\begin{equation}
(\phi \circ f)(m) - (\phi \circ f)(n) = \phi(f(m)) - \phi(f(n)) = \phi(10) - \phi(6)  = 11 - 9 = 2
\end{equation}
und
\begin{equation}
(\phi \circ f)(p) - (\phi \circ f)(q) = \phi(f(p)) - \phi(f(q)) = \phi(4)  - \phi(2)  = 7 - 3  = 4
\end{equation}
und damit
\begin{equation}
(\phi \circ f)(m)  - (\phi \circ f)(n)  \not > (\phi \circ f)(p) - (\phi \circ f)(q)
\end{equation}
Damit bleibt die Aussage
\begin{equation}
f(m) - f(n) > f(p) - f(q)
\end{equation} also nicht für alle $\phi \in \Phi$ wahr, sondern wird für das hier betrachtete $\phi$ falsch.

*Bedeutsamkeit bei Intervallskala*

$f$ sei eine Intervallskala. Dann ist die Menge $\Phi$ der zulässigen Transformationen
von $f$ die Menge der positiv linear-affinen Funktionen. Für jede Transformation
von $\phi \circ f$ von $f$ mit $\phi \in \Phi$ gilt mit $\alpha > 0, \beta \in \mathbb{R}$
also
\begin{align}
\begin{split}
f(m) - f(n) > f(p) - f(q)
& \Leftrightarrow
\alpha(f(m) - f(n)) + \beta - \beta > \alpha(f(p) - f(q)) + \beta - \beta \\
& \Leftrightarrow
(\alpha f(m) + \beta) - (\alpha f(n) + \beta) > (\alpha f(p) + \beta) - (\alpha f (q) + \beta) \\
& \Leftrightarrow
(\phi \circ f)(m)  - (\phi \circ f)(n)  > (\phi \circ f)(p) - (\phi \circ f)(q).
\end{split}
\end{align}

*Bedeutsamkeit bei Verhältnisskala*

$f$ sei eine Verhältnisskala. Dann ist die Menge $\Phi$ der zulässigen Transformationen
von $f$ die Menge der Ähnlichkeitstransformationen. Für jede Transformation
von $\phi \circ f$ von $f$ mit $\phi \in \Phi$ gilt mit $\alpha > 0$ also
\begin{align}
\begin{split}
f(m) - f(n) > f(p) - f(q)
& \Leftrightarrow
\alpha(f(m) - f(n)) > \alpha(f(p) - f(q)) \\
& \Leftrightarrow
\alpha f(m)  - \alpha f(n)  > \alpha f(p) - \alpha f(q) \\
& \Leftrightarrow
(\phi \circ f)(m)  - (\phi \circ f)(n)  > (\phi \circ f)(p) - (\phi \circ f)(q).
\end{split}
\end{align}

:::

Die betreffende Aussage ist hier "im Allgemeinen" nicht bedeutsam, wenn $f$ eine
Ordinalskala ist: es gibt durchaus monoton steigende Funktionen, für die die
Aussage bedeutsam ist (z.B. die positiv linear-affinen Funktionen), die Aussage
bleibt aber aber nicht bei Transformation von $f$ durch alle monoton steigenden
Funktionen wahr, wie durch ein Gegenbeispiel im Beweis von
@thm-bedeutsamkeit-von-größenvergleichen-von-differenzen gezeigt wurde.


:::{#thm-bedeutsamkeit-von-verhältnisaussagen}
## Bedeutsamkeit von Verhältnisaussagen
$(\mathcal{M},\mathcal{N},f)$ sei eine Skala. Dann ist für $m,n \in M$ und $c\neq 0$ die Aussage
\begin{equation}
\frac{f(m)}{f(n)} = c
\end{equation}
im Allgemeinen nicht bedeutsam, wenn $f$ eine Ordinalskala oder eine
Intervallskala ist und bedeutsam, wenn $f$ eine Verhältnisskala ist.

:::

:::{.proof}

Wir zeigen zunächst die Bedeutsamkeit bei Verhältnisskala. Durch Angabe eines Gegenbeipiels
zeigen wir dann die Nicht-Bedeutsamkeit bei Intervallskala. Die Nichtbedeutsamkeit bei
Ordinalskala folgt dann direkt, weil alle positiv linear-affine Funktionen monotone Funktionen sind,
das Gegenbeispiel aber zeigt, dass es positiv linear-affine Funktionen gibt, für
die die Aussage nicht bedeutsam ist.

*Bedeutsamkeit bei Verhältnisskala*

$f$ sei eine Verhältnisskala. Dann ist die Menge $\Phi$ der zulässigen Transformationen
von $f$ die Menge der Ähnlichkeitstransformationen. Für jede Transformation
von $\phi \circ f$ von $f$ mit $\phi \in \Phi$ gilt mit $\alpha > 0$ also
\begin{align}
\begin{split}
\frac{f(m)}{f(n)} = c
\Leftrightarrow
\alpha \left(\frac{f(m)}{f(n)}\right) = \alpha c
\Leftrightarrow
\frac{\alpha f(m)}{\alpha f(n)} =  c
 \Leftrightarrow
\frac{(\phi \circ f)(m)}{(\phi \circ f)(n)} =  c.
\end{split}
\end{align}

*Nicht-Bedeutsamkeit bei Intervallskala*

Wir betrachten die Aussage
\begin{equation}
\frac{f(m)}{f(n)} = c
\Leftrightarrow
f(m) = c f(n)
\end{equation}
Dann gilt im Allgemeinen für eine positiv linear-affine Funktion $\phi$, dass
\begin{equation}
(\phi \circ f)(m) \neq c (\phi \circ f) (n)
\end{equation}
Zum Beispiel seien $f(m) := 2, f(n) := 1$ (also $c = 2$) und $\phi$ sei die positiv linear-affine Funktion
\begin{equation}
\phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \alpha x + \beta \mbox{ mit } \alpha := 1 \mbox{ und } \beta := 1.
\end{equation}
Dann gilt offenbar
\begin{equation}
\frac{f(m)}{f(n)} = \frac{2}{1} = 2
\end{equation}
also
\begin{equation}
f(m) = 2f(n),
\end{equation}
aber
\begin{equation}
(\phi \circ f)(m) \neq 2 (\phi \circ f) (n)
\end{equation}
denn
\begin{equation}
(\phi \circ f)(m) = \alpha f(m) + \beta = 1\cdot 2 + 1 = 3
\end{equation}
und
\begin{equation}
2(\phi \circ f)(n) =  2(\alpha f(n) + \beta) = 2\alpha f(n) + 2 \beta =  2\cdot 1 + 2 \cdot 1 = 4.
\end{equation}
:::

@thm-bedeutsamkeit-von-verhältnisaussagen formalisiert die Beobachtung,
dass Verhältnisse von Temperaturen bei Wechsel  er Messkala von Celsius zu
Fahrenheit nicht konstant bleiben (vgl. @sec-einführung-und-überblick).


### Bedeutsame Statistiken {-}

:::{#def-bedeutsame-statistik}

## Bedeutsame Statistik
$(\mathcal{M}, \mathcal{N},f)$ sei eine Skala und es seien $m_1,...,m_n \in M$, wobei
$M$ die dem qualitativen Relationssystem $\mathcal{M}$ zugrundeliegende Menge bezeichne.
$\phi$ sei eine zulässige Transformation von $f$ und $\gamma$ sei eine *reellwertige Statistik*,
d.h. eine Abbildung der Form
\begin{equation}
\gamma : f(M)^n \subset \mathbb{R}^n \to \mathbb{R}, (f(m_1),...,f(m_n)) \mapsto \gamma(f(m_1),...,f(m_n))
\end{equation}
$\gamma$ wird eine *bedeutsame Statistik* genannt, wenn
\begin{equation}
\phi\left(\gamma\left(f(m_1),...,f(m_n)\right) \right) = \gamma\left(\phi(f(m_1)),...,\phi(f(m_n))\right)
\end{equation}
:::

Für den Wert einer bedeutsamen Statistik ist es nach @def-bedeutsame-statistik
also irrelevant, ob eine Skalentransformation auf der Ebene der numerischen Repräsentationswerte
oder auf der Ebene der Statistikwerte durchgeführt wird. Die hier verwendete
Definition der bedeutsamen Statistik folgt @saint-mont2011. @narens2002 entwickelt
eine generalisierte und grundlegendere Theorie der bedeutsamen Statistiken.

:::{#thm-bedeutsamkeit-der-summe}

## Bedeutsamkeit der Summe
Die Summe
\begin{equation}
\gamma : f(M)^n \to \mathbb{R}, (f(m_1),...,f(m_n)) \mapsto \gamma(f(m_1),...,f(m_n)) := \sum_{i=1}^n f(m_i)
\end{equation}
der Werte $f(m_1),...,f(m_n)$
ist im Allgemeinen keine bedeutsame Statistik, wenn $f$ eine Ordinalskala oder
Intervallskala ist und eine bedeutsame Statistik, wenn $f$ eine Verhältnisskala ist.
:::

:::{.proof}

*Bedeutsamkeit bei Verhältnisskala*

Es sei $(\mathcal{M}, \mathcal{N},f)$ sei eine Verhältnisskala und $\phi$ eine
zulässige Transformation von $f$, also eine Ähnlichkeitstransformation der Form
\begin{equation}
\phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \alpha x \mbox{ mit } \alpha > 0
\end{equation}
Dann gilt
\begin{align}
\begin{split}
\phi(\gamma(f(m_1),..., f(m_n)))
& = \phi\left(\sum_{i=1}^n f(m_i)\right)        \\
& = \alpha\sum_{i=1}^n f(m_i)                   \\
& = \sum_{i=1}^n \alpha f(m_i)                  \\
& = \sum_{i=1}^n \phi(f(m_i))                   \\
& = \gamma(\phi(f(m_1)), ...,\phi(f(m_n)))      \\
\end{split}
\end{align}


*Nicht-Bedeutsamkeit bei Ordinal- und Intervallskala*

Wir zeigen durch Angabe eines Gegenbeispiels, dass die Summe im Allgemeinen
bei Intervallskalen keine bedeutsame Statistik ist. Da die zulässigen Transformationen
der Intervallskalen monotone Funktionen sind, ist die Summe dann auch bei
Ordinalskalen keine bedeutsame Statistik, da auch hier das Gegenbeispiel greift.

Für $n := 2$ seien $f(m_1) = 1$ und $f(m_2) = 2$ und $\phi$ sei die positiv linear-affine Funktion
\begin{equation}
\phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \alpha x + \beta \mbox{ mit } \alpha := 1 \mbox{ und } \beta := 1.
\end{equation}
Dann gilt
\begin{equation}
\phi(\gamma(f(m_1),f(m_2)))
= \phi(f(m_1) + f(m_2))
= \phi(1 + 2)
= \phi(3)
= 1\cdot 3 + 1
= 4.
\end{equation}
Es gilt aber auch
\begin{equation}
\gamma(\phi(f(m_1),\phi(f(m_2))))
= \gamma(\phi(1),\phi(2))
= \gamma(1 \cdot 1 + 1, 1 \cdot 2 + 1)
= \gamma(2,3)
= 2 + 3
= 5.
\end{equation}
Also gilt
\begin{equation}
\phi(\gamma(f(m_1),f(m_2))) = 4 \neq 5 = \gamma(\phi(f(m_1),\phi(f(m_2)))).
\end{equation}

:::{#thm-bedeutsamkeit-des-mittelwerts}
## Bedeutsamkeit des Mittelwerts
Der Mittelwert
\begin{equation}
\gamma : f(M)^n \to \mathbb{R}, (f(m_1),...,f(m_n)) \mapsto \gamma(f(m_1),...,f(m_n)) := \frac{1}{n}\sum_{i=1}^n f(m_i)
\end{equation}
ist im Allgemeinen keine bedeutsame Statistik, wenn $f$ eine Ordinalskala ist und
eine bedeutsame Statistik, wenn $f$ eine Intervall- oder Verhältnisskala ist.
:::

:::{.proof}

*Bedeutsamkeit bei Verhältnisskala*

Es sei $(\mathcal{M}, \mathcal{N},f)$ sei eine Verhältnisskala und $\phi$ eine
zulässige Transformation von $f$, also eine Ähnlichkeitstransformation der Form
\begin{equation}
\phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \alpha x \mbox{ mit } \alpha > 0
\end{equation}
Dann gilt
\begin{align}
\begin{split}
\phi(\gamma(f(m_1),..., f(m_n)))
& = \phi\left(\frac{1}{n}\sum_{i=1}^n f(m_i)\right)         \\
& = \alpha\frac{1}{n}\sum_{i=1}^n f(m_i)                    \\
& = \frac{1}{n} \sum_{i=1}^n \alpha f(m_i)                  \\
& = \frac{1}{n} \sum_{i=1}^n \phi(f(m_i))                   \\
& = \gamma(\phi(f(m_1)), ...,\phi(f(m_n)))                  \\
\end{split}
\end{align}

*Bedeutsamkeit bei Intervallskala*

Es sei $(\mathcal{M}, \mathcal{N},f)$ sei eine Intervallskala und $\phi$ eine
zulässige Transformation von $f$, also eine positiv linear-affine der Form
\begin{equation}
\phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \alpha x + \beta \mbox{ mit } \alpha > 0
\end{equation}
Dann gilt
\begin{align*}
\begin{split}
\phi(\gamma(f(m_1),..., f(m_n)))
  = \phi\left(\frac{1}{n}\sum_{i=1}^n f(m_i)\right)
& = \alpha\left(\frac{1}{n}\sum_{i=1}^n f(m_i)\right) +  \beta                   \\
& = \frac{1}{n}\sum_{i=1}^n \alpha f(m_i) + \frac{1}{n}\sum_{i=1}^n\beta         \\
& = \frac{1}{n}\left(\sum_{i=1}^n \alpha f(m_i) + \sum_{i=1}^n \beta \right)     \\
& = \frac{1}{n}\sum_{i=1}^n (\alpha f(m_i) + \beta)                              \\
& = \frac{1}{n}\sum_{i=1}^n \phi(f(m_i))
  = \gamma(\phi(f(m_1)), ...,\phi(f(m_n)))
\end{split}
\end{align*}

*Nicht-Bedeutsamkeit bei Ordinalskala*

Für $f(M) \subset \mathbb{R}_{>0}$ und $n := 2$ seien $f(m_1) = 1$ und $f(m_2) = 3$
und es sei $\phi$ die monotone Funktion
\begin{equation}
\phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := x^2.
\end{equation}
Dann gilt
\begin{equation}
\phi(\gamma(f(m_1),f(m_2)))
= \phi\left(\frac{1}{2}(1+3)\right)
= \phi(2)
= 2^2
= 4.
\end{equation}
Es gilt aber auch
\begin{equation}
\gamma(\phi(f(m_1),\phi(f(m_2))))
= \gamma(\phi(1),\phi(3))
= \gamma\left(1^2,3^2\right)
= \frac{1}{2}(1 + 9)
= 5.
\end{equation}
Also gilt
\begin{equation}
\phi(\gamma(f(m_1),f(m_2))) = 4 \neq 5 = \gamma(\phi(f(m_1),\phi(f(m_2)))).
\end{equation}
:::

\normalsize

## Repräsentation geordneter Strukturen {#sec-repräsentation-geordneter-strukturen}

### Ordnungsrelationen {-}

Die Repräsentation einer strengen schwachen Ordnung ist ein Beispiel für die
strukturerhaltende Repräsentation von *Ordnungsrelationen*. Ordnungsrelationen
sind Binärrelationen und formalisieren Intuitionen von Rangfolgen wie z.B. "größer als",
"mehr als" oder "wird präferiert über". Vertraute Ordnungsrelationen sind $>$ und
$\ge$ auf der Menge der reellen Zahlen. Die $>$ und $\ge$ Relationen auf den
reellen Zahlen haben dabei unterschiedliche Eigenschaften. Zum Beispiel
ist die Relation $>$ nicht reflexiv, weil $x \not{>} x$, also $(x,x) \notin \, >$,
aber die Relation $\ge$ ist reflexiv, weil $x \ge x$, also $(x,x) \in\, \ge$.
 Die Theorie der Ordnungsrelationen versucht, Eigenschaften wie die (Ir)Reflexivität
von $>$ und $\ge$ zu abstrahieren und auf andere, nicht numerische Mengen, zu übertragen.
Je nach den konkreten Eigenschaften einer Ordnungsrelation definiert man zum Beispiel
folgende Ordnungsrelationen (vgl. @roberts1984, S. 15):

<!--

###############################################################################
                     UPDATE VOM OFFICE RECHNER 24.02.2026
###############################################################################
\tiny

| Eigenschaft          | Quasiordnung | Schwache Ordnung | Einfache Ordnung           | Strikte einfache Ordnung | Strikte schwache Ordnung | Partielle Ordnung | Strikte partielle Ordnung |
|:---------------------|:------------:|:----------------:|:--------------------------:|:------------------------:|:------------------------:|:-----------------:|:-------------------------:|
| Reflexiv             | $\bullet$    | $\bullet$        | $\bullet$                  |                          |                          | $\bullet$         |                           |
| Symmetrisch          |              |                  |                            |                          |                          |                   |                           |
| Transitiv            | $\bullet$    | $\bullet$        | $\bullet$                  | $\bullet$                | $\bullet$                | $\bullet$         | $\bullet$                 |
| Asymmetrisch         |              |                  |                            | $\bullet$                |                          |                   |                           |
| Antisymmetrisch      |              |                  | $\bullet$                  |                          |                          | $\bullet$         |                           |
| Negativ transitiv    |              |                  |                            |                          | $\bullet$                |                   |                           |
| Vollständig          |              | $\bullet$        | $\bullet$                  | $\bullet$                |                          |                   |                           | -->


<!-- Lebensnahe Beispiele (mit Begründung)
Quasiordnung (reflexiv, transitiv)

Beispiel: „Ist mindestens so geeignet wie“ bei Bewerbungen nach Mindestkriterien (z. B. Führerschein, Sprachlevel, Zertifikatliste).
Definiere: Person A ≤ Person B, wenn A alle Mindestkriterien erfüllt, die B erfüllt (A hat eine Superset-Kriteriensammlung).
Warum? Reflexiv (A erfüllt alles, was A erfüllt), transitiv (wenn A alles von B und B alles von C erfüllt, dann A alles von C). Antisymmetrie kann scheitern, wenn zwei verschiedene Personen exakt dieselbe Kriteriensammlung erfüllen.

Schwache Ordnung / weak order (totale Präordnung: reflexiv, transitiv, vollständig)

Beispiel: Restaurantbewertungen mit 1–5 Sternen: A ≤ B, wenn Sterne(A) ≤ Sterne(B).
Warum? Vollständig (jede zwei Restaurants haben Sternewerte, die vergleichbar sind), reflexiv/transitiv klar. Nicht antisymmetrisch, weil zwei verschiedene Restaurants gleich viele Sterne haben können.

Einfache Ordnung (lineare/Totalordnung: reflexiv, antisymmetrisch, transitiv, vollständig)

Beispiel: Rangordnung nach Geburtszeitpunkt (Datum+Uhrzeit) in einem Register: A ≤ B, wenn A nicht später geboren wurde als B.
Warum? Vollständig (für zwei Zeitpunkte gilt immer ≤ oder ≥), antisymmetrisch (wenn beide nicht später als der andere sind, ist es derselbe Zeitpunkt), transitiv/reflexiv klar.

Strikte einfache Ordnung (strikte lineare Ordnung: irreflexiv, transitiv, vollständig)

Beispiel: „Ist früher geboren als“ anhand des Geburtszeitpunkts: A < B, wenn A strikt früher geboren wurde als B.
Warum? Irreflexiv (niemand früher als er selbst), transitiv (früher als ist transitiv), vollständig im strikten Sinn (für A≠B gilt A<B oder B<A).

Strikte schwache Ordnung / strict weak order (irreflexiv, transitiv, negativ transitiv)

Beispiel: Sortieren von Dateien nach Dateigröße: A < B, wenn Größe(A) < Größe(B).
Warum? Irreflexiv/transitiv klar. Nicht vollständig: zwei Dateien gleicher Größe sind weder A<B noch B<A (unvergleichbar). Die dadurch entstehende „Gleichwertigkeit“ („gleich groß“) verhält sich konsistent; das ist genau der strict-weak-order-Use-Case bei Sortierkriterien.

Partielle Ordnung (Halbordnung: reflexiv, antisymmetrisch, transitiv)

Beispiel: „Kann ohne Umwege aufeinander aufbauen“ bei Aufgaben mit Abhängigkeiten: A ≤ B, wenn Aufgabe A Voraussetzung (direkt oder indirekt) für B ist, oder A=B.
Warum? Reflexiv per „oder A=B“, transitiv durch Ketten von Abhängigkeiten, antisymmetrisch (wenn A Voraussetzung von B und B Voraussetzung von A, dann sind sie inhaltlich dieselbe Aufgabe bzw. wäre ein Zyklus, der in einer sauberen Abhängigkeitsstruktur nicht zugelassen ist). Nicht vollständig, weil viele Aufgaben unabhängig sind.


\normalsize

Für jeder der in der Tabelle angegebenen Ordnungsrelationen können das
Repräsentationsproblem und das Eindeutigkeitsproblem der Ordinalmessung untersucht
werden. Wir betrachten hier der Einfachheit halber nur den Fall der Ordinalmessung
bei Vorliegen einer **strengen schwachen Ordnung** (strict weak order).


# Vorbemerkungen

\textcolor{darkblue}{Ordinalmessungen}

\footnotesize
Ziel von Ordinalmessungen ist es, den Elementen einer Menge $M$, die in einer gegebenen
Ordnungsrelation $R$ stehen, durch eine Funktion $f$ Zahlen in $\mathbb{R}$ so zuzuordnen, dass gilt
\begin{equation}
(m,n) \in R \Leftrightarrow f(m) > f(n)
\end{equation}
oder, in Abhängigkeit von $R$, auch
\begin{equation}
(m,n) \in R \Leftrightarrow f(m) \ge f(n).
\end{equation}
Da die Struktur von $R$ durch den Homomorphismus per Definition nicht verändert wird, die Funktion
$f$ also keine zusätzliche Information im Vergleich zu $R$ enkodiert, stellt sich die
Frage, was durch eine Ordinalmessung bei bekannter Ordnungsrelation $R$ wissenschaftlich überhaupt gewonnen wird.

Mögliche Antworten auf diese Frage sind folgende:

*  $(m,n) \in R \vee (m,n) \notin R$ sind binäre Aussagen, $f(m), f(n) \in \mathbb{R}$
  sind Zahlen. Die Übersetzung einer qualitativen Ordnungsrelation erlaubt es also,
  die Methoden der angewandten  Mathematik auf die numerisch repräsentierte
  Ordnungsrelation anzuwenden. Die den Elementen von $M$ zugeordneten Funktionswerte
  $f(m) \in \mathbb{R}$ können zum Beispiel im Sinne von abhängigen Variablen mit anderen den
  Elementen von $M$ zugeordneten Funktionswerten $g(m) \in \mathbb{R}$ als unabhängige
  Variablen statistisch in Bezug gesetzt werden.
* In ähnlicher Weise sind Antworten auf Fragen der relativen Position eines $m \in M$
  unter Umständen anhand der Menge der $f(m)$ leichter zu evaluieren. Zum Beispiel
  müssen für das ranghöchste oder rangniedrigste $m \in M$ lediglich
  $\mbox{argmin}_{m \in M}f(m)$ und $\mbox{argmax}_{m \in M}f(m)$ evaluiert und
  nicht alle paarweisen Vergleiche gezogen werden.
* $(m,n) \in R \vee (m,n) \notin R$ sind $|M^2|$ Aussagen,
  $f(m)$ sind $|M|$ Zahlen. Die numerisch repräsentierte Ordnungsrelation
  kann also eventuell mit weniger Aufwand enkodiert werden.
  # Strenge schwache Ordnungen

:::{#def-Asymmetrie]
Eine Binärrelation $R$ auf einer Menge $M$ heißt *asymmetrisch}, wenn
für alle $(m,n) \in R$ gilt, dass $(n,m) \notin R$ ist.
:::

\footnotesize
Bemerkungen und Beispiele

* Die $>$ Relation auf $\mathbb{R}$ ist asymmetrisch, weil für alle $x,y\in \mathbb{R}$ gilt, dass wenn $x>y$ gilt, auch $y\not{>}x$ gilt.
* Die $\ge$ Relation auf $\mathbb{R}$ ist nicht asymmetrisch, weil für $x = y$ bei $x\ge y$ nicht gilt, dass $y\not{\ge}x$.
* Für $M := \{a,b,c\}$ ist die Relation
\begin{equation}
R := \{(a,b), (a,c), (b,c)\}
\end{equation}
asymmetrisch, weil gilt, dass
\begin{align}
\begin{split}
(a,b) \in R & \Rightarrow (b,a) \notin R \\
(a,c) \in R & \Rightarrow (c,a) \notin R \\
(b,c) \in R & \Rightarrow (c,b) \notin R \\
\end{split}
\end{align}
* Die (rationale, normative, idealisierte) Präferenzrelation "wird präferiert über" ist asymmetrisch.
* Die Präferenzrelation "wird präferiert über oder wird gleichermaßen präferiert" ist nicht asymmetrisch.

# Strenge schwache Ordnungen

:::{#def-Negative Transitivität]

Eine Binärrelation $R$ auf einer Menge $M$ heißt *negativ transitiv}, wenn
für alle $m,n,p\in M$ gilt, dass aus $(m,n) \notin R$ und $(n,p)\notin R$ folgt,
dass $(m,p) \notin R$ ist.
:::

\footnotesize
Bemerkungen und Beispiele

*  Die $>$ Relation auf $\mathbb{R}$ ist negativ transitiv, weil für alle $x,y,z\in \mathbb{R}$
gilt, dass aus $x\not{>}y$ und $y\not{>}z$ folgt, dass $x\not{>}z$. Seien zum Beispiel
$x = 1, y = 3$ und $z = 6$. Offenbar gelten dann $x \not{>} y$ und $y \not{>} z$ und auch
$x \not{>} z$.
* Für $M := \{a,b,c\}$ ist die Relation
\begin{equation}
R := \{(a,b), (a,c), (b,c)\}
\end{equation}
negativ transitiv. Hier gilt zum Beispiel, dass mit $(c,b)\notin R$ und $(b,a) \notin R$
auch $(c,a) \notin R$ ist. Für einen vollständigen Nachweis der negativen Transitivität
muss allerdings gezeigt werden, dass dies für alle relevanten Paare von Tupeln in $M\times M$ gilt.
* Die (rationale, normative, idealisierte) Präferenzrelation "wird präferiert über" ist negativ transitiv.



$\hfill \Box$


###############################################################################
                     UPDATE VOM OFFICE RECHNER 24.02.2026
###############################################################################

-->


### Strenge schwache Ordnungen {#sec-strenge-schwache-ordnungen}

:::{#def-strenge-schwache-ordnung}
## Strenge schwache Ordnung
$M$ sei eine Menge und $R$ sei eine Binärrelation auf $M$. $R$ heißt *strenge
schwache Ordnung* auf $M$, wenn $R$ asymmetrisch und negativ transitiv ist.
:::


Wir haben in @sec-relationen gesehen, dass die $>$ Relation auf $\mathbb{R}$
asymmetrisch und negativ transitiv ist. Die $>$ Relation ist also eine strenge
schwache Ordnung auf $\mathbb{R}$. Wir haben in @sec-relationen auch gesehen,
dass die $\ge$ Relation auf $\mathbb{R}$ nicht  asymmetrisch ist. Die $\ge$ Relation
ist also keine strenge schwache Ordnung auf $\mathbb{R}$.

Folgende Eigenschaften einer strengen schwachen Ordnung werden für den Nachweis
ihrer Repräsentation benötigt.

:::{#thm-irreflexivität-und-transitivität-strenger-schwacher-ordnungen}
## Irreflexivität und Transitivität einer strengen schwachen Ordnungen
Eine strenge schwache Ordnung $R$ auf einer Menge $M$ ist irreflexiv und transitiv.
:::

:::{.proof}

Zum Nachweis der Irreflexivität ist zu zeigen, dass für alle $m \in M$ gilt, dass
$(m,m)\notin R$ ist. Zum Beweis durch Widerspruch sei $(m,m) \in R$. Dann würde
mit der Asymmetrie von  $R$ gelten, dass mit $(m,m) \in R$ auch $(m,m) \notin R$
ist (um das einzusehen ersetze man in der Definition der Asymmetrie $n$ durch $m$)
und dies ist ein Widerspruch. Also kann $(m,m) \in R$ nicht gelten und es muss $(m,m) \notin R$
gelten. Also ist $R$ irreflexiv.

Zum Nachweis der Transitivität von $R$ ist zu zeigen, dass für $(m,n) \in R$ und
$(n,p) \in R$ auch $(m,p) \in R$ gilt. Es seien also $(m,n) \in R$ und $(n,p) \in R$.
Zum Beweis durch Widerspruch sei $(m,p) \notin R$. Die Asymmetrie von $R$ impliziert,
dass aus $(n,p) \in R$ folgt, dass $(p,n) \notin R$. Die negative Transitivität von
$R$ impliziert nun für $(m,p) \notin R$ und $(p,n) \notin R$, dass $(m,n) \notin R$,
was ein Widerspruch zur Voraussetzung $(m,n) \in R$ ist. Also kann $(m,p) \notin R$
nicht gelten und es muss für $(m,n) \in R$ und $(n,p) \in R$ gelten, dass $(m,p) \in R$
ist. Also ist $R$ transitiv.
:::


### Repräsentation und Eindeutigkeit {#sec-repräsentation-und-eindeutigkeit}
:::{#thm-repräsentation-strenger-schwacher-ordnungen}
## Repräsentation strenger schwacher Ordnungen
$\mathcal{M} := (M,R)$ sei ein qualitatives Relationssystem mit einer endlichen Menge
$M$ und einer Binärrelation $R$ auf $M$ und $\mathcal{N} := (\mathbb{R},>)$ sei
ein numerische Relationssystem. Dann existiert ein Homomorphismus $f$ von
$\mathcal{M}$ nach $\mathcal{N}$, also eine Funktion der Form
\begin{equation}
f : M \to \mathbb{R}, m \mapsto f(m)
\end{equation}
mit der Eigenschaft
\begin{equation}
(m,n) \in R \Leftrightarrow f(m) > f(n)
\end{equation}
dann und nur dann, wenn $R$ eine strenge schwache Ordnung ist.
:::

:::{.proof}
Wir zeigen in einem ersten Schritt, dass wenn ein Homomorphismus von $(M,R)$ nach
$(\mathbb{R},>)$ existiert, $R$ eine strenge schwache Ordnung sein muss. In einem
zweiten Schritt zeigen wir dann, dass wenn $R$ eine strenge schwache Ordnung ist,
ein Homomorphismus von $(M,R)$ nach $(\mathbb{R}, >)$ konstruiert werden kann.

*Ein Homomorphismus $f$ von $(M,R)$ nach $(\mathbb{R},>)$ existiert $\Rightarrow$ $R$ muss eine strenge schwache Ordnung sein*

Wir zeigen, dass aus der Existenz eines Homomorphismus $f$ von $(M,R)$ nach
$(\mathbb{R},>)$ die Asymmetrie und die negative Transitivität von $R$ folgen.

Zum Nachweis der Asymmetrie sei $(m,n) \in R$. Dann gilt nach Voraussetzung,
dass $f(m) > f(n)$. Da die $>$ Relation auf $\mathbb{R}$ asymmetrisch ist, gilt dann
$f(n) \not{>} f(m)$ und dementsprechend auch $(n,m) \notin R$. Also folgt für
alle $(m,n) \in R$, dass $(n,m) \notin R$ ist und damit ist $R$ asymmetrisch.

Zum Nachweis der negativen Transitivität seien $(m,n)\notin R$ und $(n,p)\notin R$.
Dann gelten $f(m)\not{>}f(n)$ und $f(n)\not{>}f(p)$. Dann folgt aber auch
$f(m)\not{>}f(p)$ und dementsprechend auch $(m,p) \notin R$.

Die Existenz eines Homomorphismus von $(M,R)$ nach $(\mathbb{R},>)$ impliziert also,
dass $R$ eine strenge schwache Ordnung auf $M$ ist.

*$R$ ist eine strenge schwache Ordnung auf $M$ $\Rightarrow$ Ein Homomorphismus $f$ kann konstruiert werden*

Wir zeigen, dass für ein qualitatives Relationssystem $(M,R)$ aus einer endlichen
Menge $M$ und einer strengen schwachen Ordnung $R$ ein Homomorphismus von $(M,R)$
nach $(\mathbb{R},>)$ konstruiert werden kann. Wir definieren dazu eine Funktion $f$
wie folgt:
\begin{equation}
f : M \to \mathbb{R}, m \mapsto f(m) := |\{q \in M|(m,q) \in R\}|
\end{equation}
Der einem Element $m \in M$ zugeordnete Funktionswert $f(m)$ soll also die Kardinalität
der Menge der $q \in M$ sein, für die gilt, dass $(m,q) \in R$.

Zum Beispiel sei für $M := \{a,b,c,d\}$ die strenge schwache Ordnung
\begin{equation}
R := \{(a,c), (a,d), (b,c), (b,d), (c,d)\}
\end{equation}
definiert. Dann gilt für $f$, dass
\begin{align}
\begin{split}
f(a) = 2 & \mbox{, weil } \{q \in M|(a,q) \in R\} = \{c,d\}    \\
f(b) = 2 & \mbox{, weil } \{q \in M|(b,q) \in R\} = \{c,d\}    \\
f(c) = 1 & \mbox{, weil } \{q \in M|(c,q) \in R\} = \{d\}      \\
f(d) = 0 & \mbox{, weil } \{q \in M|(d,q) \in R\} = \emptyset  \\
\end{split}
\end{align}
Wir zeigen nun, dass ein solch definiertes $f$ die Forderung $(m,n) \in R \Leftrightarrow f(m)>f(n)$
erfüllt. Dazu zeigen wir erstens, dass aus $(m,n) \in R$ folgt, dass $f(m)>f(n)$ ist und zweitens,
dass (2) as $f(m)>f(n)$ folgt, dass $(m,n) \in R$ ist.

\noindent *(1) $(m,n) \in R \Rightarrow f(m)>f(n)$*

Es sei $(m,n) \in R$. Weil $R$ transitiv ist, impliziert $(n,q) \in R$ dann $(m,q) \in R$ für jedes $q \in M$.
Die Anzahl der $q$ mit $(m,q) \in R$ ist also mindestens so groß wie die Anzahl der $q$ mit $(n,q) \in M$. Es gilt also
\begin{equation}
|\{q \in M|(m,q) \in R\}| \ge |\{q \in M|(n,q) \in R\}| \Leftrightarrow f(m) \ge f(n)
\end{equation}
Weiterhin gilt aber auch
\begin{equation}
|\{q \in M|(m,q) \in R\}| \neq |\{q \in M|(n,q) \in R\}| \Leftrightarrow f(m) \neq f(n),
\end{equation}
weil nach Voraussetzung $(m,n) \in R$, aber aufgrund der Irreflexitivität von $R$ dann $(n,n) \notin R$ gilt.
Für $n$ gilt also
\begin{equation}
n \in \{q \in M|(m,q) \in R\}, \mbox{ aber } n \notin \{q \in M|(n,q) \in R\}.
\end{equation}
Damit können die Kardinalitäten dieser beiden Mengen nicht gleich sein. Also gilt
$(m,n) \in R \Rightarrow f(m)>f(n)$.

\noindent *(2) $f(m)>f(n) \Rightarrow (m,n) \in R$*

Im Folgenden nutzen wir Kontrapositionsbeweise, also die logische Äquivalenz
von $A \Rightarrow B$ und $\neg B \Rightarrow \neg A$, z.B.
\begin{equation*}
\mbox{Es regnet } (A)  \Rightarrow \mbox{ Die Strasse ist nass } (B) \Leftrightarrow \mbox{ Die Strasse ist nicht nass } (\neg B)  \Rightarrow  \mbox{ Es regnet nicht } (\neg A).
\end{equation*}
Um zu zeigen, dass aus $f(m)>f(n)$ folgt, dass $(m,n) \in R$ ist, zeigen wir also, dass
umgekehrt aus $(m,n) \notin R$ folgt, dass $f(m) \not{>} f(n)$. Dazu halten wir
fest, dass aus der negativen Transitivität von $R$ folgt, dass für $(m,n) \notin R$
aus $(n,q) \notin R$ folgt, dass $(m,q) \notin R$. Andersherum impliziert dann aber
$(m,q) \in R$, dass $(n,q) \in R$ ist und damit
\begin{equation*}
(m,n) \notin R \Rightarrow |\{q \in M|(n,q) \in R\}| \ge |\{q \in M|(m,q) \in R\}| \Leftrightarrow f(n) \ge f(m) \Leftrightarrow f(m) \not{>} f(n).
\end{equation*}
Damit ist bewiesen, dass aus $(m,n) \notin R$ folgt, dass $f(m) \not{>} f(n)$
und äquivalent, dass aus $f(m)>f(n)$ folgt, dass $(m,n) \in R$ ist.
:::


@thm-repräsentation-strenger-schwacher-ordnungen beantwortet das Repräsentationsproblem
für die hier betrachteten Fälle von $\mathcal{M}$ und $\mathcal{N}$. Für eine
strenge schwachen Ordnung auf einer endlichen Menge sind $(M,R)$ und $(\mathbb{R},>)$
also homomorph.

:::{#thm-eindeutigkeit-der-repräsentation-strenger-schwacher-ordnungen}
## Eindeutigkeit der Repräsentation strenger schwacher Ordnungen
$\mathcal{M} := (M,R)$ sei ein qualitatives Relationssystem aus einer endlichen Menge
$M$ und einer strengen schwachen Ordnung $R$ auf $M$, $\mathcal{N} := (\mathbb{R},>)$ sei
ein numerische Relationssystem und es sei $f \in \mathcal{H}(\mathcal{M},\mathcal{N})$.
Dann ist $(\mathcal{M},\mathcal{N},f)$ eine Ordinalskala.
:::

:::{.proof}

*Regularität der Repräsentation*

Wir zeigen zunächst, dass die Repräsentation $\mathcal{M} \to \mathcal{N}$ regulär ist,
um nachzuweisen, dass es zu jedem $f \in \mathcal{H}(\mathcal{M}, \mathcal{N})$ eine zulässige
Transformation gibt. Dazu nutzen wir das Theorem zur Charakterisierung regulärer Skalen
(vgl. (7) Skalenarten), welches besagt, dass eine Skala $(\mathcal{M},\mathcal{N},f)$
dann und nur dann regulär ist, wenn für alle $g \in \mathcal{H}(\mathcal{M}, \mathcal{N})$
und $m,n \in M$ gilt, dass aus $f(m) = f(n)$ folgt, dass $g(m) = g(n)$ ist. Sei also
$g \in \mathcal{H}(\mathcal{M}, \mathcal{N})$, d.h. $g$ erfüllt die Eigenschaft
\begin{equation}
g : M \to \mathbb{R}, m \mapsto g(m) \mbox{ mit } (m,n) \in R \Leftrightarrow g(m) > g(n).
\end{equation}
Sei nun weiterhin $f(m) = f(n)$. Dies impliziert, dass $(m,n) \notin R$ und $(n,m) \notin R$, weil sonst
entweder $f(m) > f(n)$ oder $f(n) > f(m)$ gelten. Wenn aber $(n,m) \notin R$ und $(m,n) \notin R$,
dann können auch weder $g(m) > g(n)$ noch $g(n) > g(m)$ gelten. Also muss $g(m) = g(n)$ gelten.

Weil  $f \in \mathcal{H}(\mathcal{M}, \mathcal{N})$ beliebig war, gilt dies für alle
$f \in \mathcal{H}(\mathcal{M}, \mathcal{N})$ und damit ist die Repräsentation
$\mathcal{M} \to \mathcal{N}$ regulär und es gibt zu jedem $g \in \mathcal{H}(\mathcal{M}, \mathcal{N})$
eine zulässige Transformation $\phi$, so dass $g = \phi \circ f$.

*Ordinalskalenart*

Wir zeigen nun, dass die Menge der zulässigen Transformationen von $(\mathcal{M},\mathcal{N},f)$
die Menge der monoton steigenden Funktionen ist. Dazu zeigen wir zunächst, dass für eine
monoton steigende Funktion $\phi$ gilt, dass $\phi \circ f \in \mathcal{H}(\mathcal{M}, \mathcal{N})$.
Abschließen zeigen wir, dass aus $\phi \circ f \in \mathcal{H}(\mathcal{M}, \mathcal{N})$ folgt, dass
$\phi$ eine monoton steigende Funktion ist. Dazu halten wir zunächst noch ein einmal fest,
dass für die Verkettung $\varphi \circ \psi$ zweier monoton steigender Funktionen $\varphi$ und $\psi$
gilt, dass ihre Verkettung wiederum monoton steigend ist, denn
\begin{equation}
x > y \Leftrightarrow \psi(x) > \psi(y) \Leftrightarrow \varphi(\psi(x)) > \varphi(\psi(y))  \Leftrightarrow (\varphi \circ \psi) (x) > (\varphi \circ \psi) (y).
\end{equation}

\noindent *(1) $\phi$ ist monoton steigend $\Rightarrow$ $\phi \circ f \in \mathcal{H}(\mathcal{M}, \mathcal{N})$*

Es gilt
\begin{equation}
(m,n) \in R \Leftrightarrow f(m) > f(n) \Leftrightarrow \phi(f(m)) > \phi(f(n)) \Leftrightarrow  (\phi \circ f)(m) >  (\phi \circ f)(n).
\end{equation}
Also ist $\phi \circ f \in \mathcal{H}(\mathcal{M}, \mathcal{N})$.

\noindent *(2) $\phi \circ f \in \mathcal{H}(\mathcal{M}, \mathcal{N}) \Rightarrow \phi$ ist monoton steigend*

Es seien $\mu := f(m) \in f(M)$ und $\nu := f(n) \in f(M)$. Dann gilt für $\phi \circ f \in \mathcal{H}(\mathcal{M}, \mathcal{N})$, dass
\begin{equation}
\mu > \nu \Leftrightarrow f(m) > f(n) \Leftrightarrow m > n  \Leftrightarrow (\phi \circ f)(m) > (\phi \circ f)(n) \Leftrightarrow \phi(\mu) > \phi(\nu).
\end{equation}
Also ist $\phi$ monoton steigend.

:::

Die Klasse der zulässigen Transformationen von $f$ sind also die monoton
steigenden Funktionen. Dass in diesem Fall $\mathcal{H}(\mathcal{M},\mathcal{N}) \neq \emptyset$ gilt,
wurde oben nachgewiesen. Strenge schwache Ordnungen sind also nicht eindeutig repräsentiert.

### Anwendungsbeispiel {-}

<!--
# Repräsentation und Eindeutigkeit
\textcolor{darkblue}{Anwendungsbeispiel}
\footnotesize

Es sei $M := \{\mbox{ROP},\mbox{HOTD},\mbox{ST4}, \mbox{OWK}, \mbox{1899}\}$
eine Menge von Serien und es sei $R$ die durch untenstehendes Schema definierte
"wird präferiert über" strenge schwache Ordnung auf $M$ eines Individuums. Dabei enkodiert
eine 1 in der $m,q$-ten Zelle, dass $m$ über $q$ präferiert wird, also $(m,q) \in R$,
eine 0 in der $m,q$-ten Zelle, dass dies nicht gilt, also $(m,q) \notin R$. Die
letzte Spalte zeigt die Funktionswerte des im Beweis der Repräsentation strenger
schwacher Ordnungen konstruierten Homomorphismus.


\tiny
\center
\begin{tabular}{l|ccccc|c}
$m \downarrow, q \rightarrow$ & ROP   & HOTD  & ST4   & OWK & 1899  & $|\{q \in M| (m,q) \in R \}|$ \\\hline
ROP                           & 0     & 0     & 0     & 1   & 1     & 2                             \\
HOTD                          & 1     & 0     & 1     & 1   & 1     & 4                             \\
ST4                           & 1     & 0     & 0     & 1   & 1     & 3                             \\
OWK                           & 0     & 0     & 0     & 0   & 0     & 0                             \\
1899                          & 0     & 0     & 0     & 1   & 0     & 1                             \\\hline
\end{tabular}

\footnotesize
\flushleft

Eine Neuanordnung von Zeilen und Spalten anhand obiger Zeilensummen lässt die Ordnung klarer erkennen:


\tiny
\center
\begin{tabular}{l|ccccc|c}
$m \downarrow, q \rightarrow$ & HOTD  & ST4   & ROP & 1899 & OWK  & $|\{q \in M| (m,q) \in R \}|$ \\\hline
HOTD                          & 0     & 1     & 1   & 1    & 1    & 4                             \\
ST4                           & 0     & 0     & 1   & 1    & 1    & 3                             \\
ROP                           & 0     & 0     & 0   & 1    & 1    & 2                             \\
1899                          & 0     & 0     & 0   & 0    & 1    & 1                             \\
OWK                           & 0     & 0     & 0   & 0    & 0    & 0                             \\\hline
\end{tabular}

\flushleft
\footnotesize

Für dieses Individuum gilt für alle $m,n\in M$, dass $m$ über $n$
präferiert wird oder dass $n$ über $m$ präferiert wird.


# Repräsentation und Eindeutigkeit
\vspace{3mm}
\textcolor{darkblue}{Anwendungsbeispiel}
\footnotesize

Elemente von $\mathcal{H}(\mathcal{M},\mathcal{N})$ im vorliegenden Beispiel

```{r, eval = F, echo = F}
library(latex2exp)
graphics.off()
dev.new()
par(
mfcol      = c(2,2),
family     = "sans",
pty        = "s",
bty        = "l",
lwd        = 1,
las        = 1,
mgp        = c(2,1,0),
font.main  = 1.2,
cex.main   = 1.3)
m          = 1:5
g          = list(0:4, c(0:4)^2, 2*c(0:4) + 1, exp(0:4))
label      = list(TeX("$f(m)$"),
                  TeX("$\\phi(f(m)), \\phi(x) := x^2$"),
                  TeX("$\\phi(f(m)), \\phi(x) := 2x + 1$"),
                  TeX("$\\phi(f(m)), \\phi(x) := exp(x)$"))

for(i in 1:4){
  plot(
  m,
  g[[i]],
  type         = "h",
  lwd          =  1,
  col          = "Black",
  ylab         = " ",
  xlab         = "",
  xaxt         = "n")
  title(label[[i]])
  axis(
  1,
  at           = m,
  labels       = c("OWK","1899", "ROP", "ST4", "HOTD"))
}

# export to pdf
dev.copy2pdf(file = file.path("8_Abbildungen/pfm_8_anwendungsbeispiel.pdf"),width = 7, height = 7)
```

-->

## Repräsentation differentieller Strukturen{#sec-repräsentation-differentieller-strukturen}

## Repräsentation extensiver Strukturen {#sec-repräsentation-extensiver-strukturen}

In @sec-hölders-theorem diskutieren wir Hölders Theorem
(@holder1901, @michell1996). Das Theorem ist ein Repräsentationstheorem für
Extensivmessungen und besagt, dass eine *Archimedisch geordnete Gruppe*  homomorph
in das quantitative Relationssystem $(\mathbb{R},>,+)$  abgebildet werden kann.
Für ein Verständnis des Theorems wird also ein Verständnis des Begriffes der
*Archimedisch geordneten Gruppe* benötigt, welches insgesamt für das Verständnis
der repräsentationstheoretischen Konzeption einer Extensivmessung nötig ist. Für führen
in diesem Abschnitt also zunächst die Begriffsbildung der Archimedisch geordneten Gruppe ein
und formulieren dann Hölders Theorem.

Das Modell der Archimedisch geordneten Gruppe als Modell für physikalische Extensivmessungen
hat einige Unzulänglichkeiten, die spätere Messtheoretiker:innen versucht haben zu verbessern
(vgl. @krantz1971). In @sec-extensive-strukturen diskutieren wir eine von
@roberts1968 vorgeschlagene Verbesserung des Modells des qualitativen Relationssystem der Extensivmessung.

In @sec-repräsentation-und-eindeutigkeit-extensiver-strukturen geben wir schließlich
das Repräsentationstheorem für extensive Strukturen an und zeigen, dass die Klasse
der zulässigen Transformationen von Homomorphismen von einer extensiven Struktur
nach $(\mathbb{R},>,+)$ die Menge der Änlichkeitstransformationen ist, dass also
Extensivmessungen auf Verhältnisskalen führen.

### Hölders Theorem {#sec-hölders-theorem}

:::{#def-vollständigkeit}
## Vollständigkeit
Eine Binärrelation  $R$ auf einer Menge $M$ heißt *vollständig*,
wenn für alle $m,n \in M$ mit $m \neq n$ gilt, dass entweder $(m,n) \in R$
oder $(n,m) \in R$ ist.
:::

Bei einer vollständigen Relation stehen also *alle ungleichen* Elemente von $M$
in Relation. Die $>$ Relation auf $\mathbb{R}$ ist vollständig, weil für alle
$x,y$ mit $x \neq y$ gilt, dass entweder $x>y$ oder $y>x$.

:::{#def-strenge-einfache-ordnung}
## Strenge einfache Ordnung
$M$ sei eine Menge und $R$ sei eine Binärrelation auf $M$. $R$ heißt
*strenge einfache Ordnung* auf $M$, wenn $R$ asymmetrisch, transitiv
und vollständig ist.
:::

Im Unterschied zur strengen einfachen Ordnung muss die strenge schwache Ordnung
nicht vollständig sein.

:::{#exm-die-g-relation-als-strenge-einfache-ordnung}
## Die $>$-Relation als strenge einfache Ordnung

Die $>$ Relation auf $\mathbb{R}$ ist eine strenge einfache Ordnung, denn $>$
ist asymmetrisch, weil für alle $x,y\in \mathbb{R}$ gilt, dass wenn $x>y$ auch $y \not{>}x$ gilt;
$>$ ist transitiv, weil für alle $x,y,z\in \mathbb{R}$ gilt, dass mit $x>y$ und $y>z$ auch $x>z$ ist;
$>$ ist vollständig, weil für alle $x,y$ mit $x \neq y$ gilt, dass entweder $x>y$ oder $y>x$.
:::

:::{#def-gruppe}
## Gruppe
Ein Tupel $(M,\circ)$ aus einer Menge $M$ und einer Operation $\circ$ auf $M$ heißt
*Gruppe*, wenn für $(M,\circ)$ gelten:

1. (Assoziativität) Für alle $m,n,p \in M$ gilt
\begin{equation}
(m \circ n) \circ p = m \circ (n \circ p).
\end{equation}
2.  (Neutrales Element). Es gibt ein *neutrales Element* $e \in M$,
so dass für alle $m\in M$ gilt, dass
\begin{equation}
m \circ e = e \circ m = m.
\end{equation}
3. (Inverse Elemente). Für alle $m \in M$ existiert ein *inverses Element*, so dass
\begin{equation}
m \circ n = n \circ m = e.
\end{equation}
:::


Da Operationen Tupel $(m,n) \in M \times M$ nach $M$ abbilden, sind Gruppen auch
immer *geschlossen*, d.h.
\begin{equation}
m \circ n \in M \mbox{ für alle } m,n \in M.
\end{equation}

:::{#exm-gruppen}
## Gruppen
$(\mathbb{R},+)$ ist eine Gruppe, wobei $0 \in \mathbb{R}$ das neutrale Element
und $-m \in \mathbb{R}$ das zu $m$ inverse Element ist. $(\mathbb{R},\cdot)$ ist
keine Gruppe, weil $1 \in \mathbb{R}$ das einzige neutrale  Element ist und
$0 \in \mathbb{R}$ damit kein inverses Element hat: es gibt  kein
$n \in \mathbb{R}$ mit $0\cdot n = n\cdot 0 = 1$. $(\mathbb{R}_{>0},\cdot)$ dagegen
ist eine Gruppe, wobei $1 \in \mathbb{R}$ das neutrale Element und
$\frac{1}{m} \in \mathbb{R}$ das zu $m$ inverse Element ist.
:::


:::{#def-natürliches-vielfaches}
## Natürliches Vielfaches
$(M,\circ)$ sei eine Gruppe und es sei $n \in \mathbb{N}$. Dann ist das
*natürliche Vielfache* eines $m\in M$ induktiv definiert als

1. $1m := m$
2. $(n+1)m := m \circ nm$
:::

:::{#exm-natürliche-vielfache}
## Natürliche Vielfache
Wir betrachten $(\mathbb{R},+)$ und wählen $n := 2$ für ein $x \in \mathbb{R}$.
Dann gilt nach obiger Definition
\begin{equation}
(2+1)x := x + 2x \mbox{ mit } 2x = (1+1)x := x + 1x \mbox{ mit } 1x = x.
\end{equation}
Es gilt hier also z.B. für $x := 2$, dass
\begin{equation}
1x = 2 \Rightarrow 2x = 2 + 2 = 4 \Rightarrow 3x := 2 + 4 = 6.
\end{equation}
Dieses Beispiel ist wichtig. Als nächstes betrachten wir $(\mathbb{R}_{>0},\cdot)$
und wählen $n := 2$ für ein $x \in \mathbb{R}$. Dann gilt nach obiger Definition
\begin{equation}
(2+1)x := x \cdot 2x \mbox{ mit } 2x = (1+1)x := x \cdot 1x \mbox{ mit } 1x = x.
\end{equation}
Es gilt hier also z.B. für $x := 2$, dass
\begin{equation}
1x = 2 \Rightarrow 2x = 2 \cdot 2 = 4 \Rightarrow 3x := 2\cdot 4 = 8.
\end{equation}
:::



:::{#thm-archimedische-eigenschaft-der-reellen-zahlen}
## Archimedische Eigenschaft der reellen Zahlen
Es seien $x,y\in \mathbb{R}$ mit $x>0$. Dann existiert ein $n \in \mathbb{N}$, so dass $nx > y$.
:::

Wir verzichten auf einen Beweis zugunsten der von @fig-archimedische-eigenschaft-der-reellen-zahlen
vermittelten Intuition. $\mathbb{R}$ wird hier im Sinne der Gruppe $(\mathbb{R},+)$
verstanden. $nx$ bezeichnet also das oben definierte natürliche Vielfache von $x$.
@thm-archimedische-eigenschaft-der-reellen-zahlen besagt insbesondere, dass unabhängig
davon, wie klein $x$ sein mag oder wie groß $y$ sein mag, dass wenn $x$ positiv ist,
ausreiched viele Kopien von $x$ größer als $y$ sind. Die Archimedische Eigenschaft
der reellen Zahlen erlaubt es also, die relativen Größen von $x$ und $y$ zu
vergleichen, indem man ermittelt, wie viele Kopien von $x$ nötig sind, um eine
Zahl größer als $y$ zu erhalten.


![Archimedische Eigenschaft der reellen Zahlen.](./_figures/905-archimedische-eigenschaft-der-reellen-zahlen.pdf){#fig-archimedische-eigenschaft-der-reellen-zahlen fig-align="center" width=80%}

:::{#def-archimedisch-geordnete-gruppe}
## Archimedisch geordnete Gruppe
$M$ sei eine Menge, $R$ sei eine Binärrelation auf $M$ und $\circ$ sei eine
Operation auf $M$. Dann heißt das Relationssystem $(M,R,\circ)$ *Archimedisch
geordnete Gruppe*, wenn für $(M,R,\circ)$ gelten:

1. (Ordnungseigenschaft) $R$ ist eine strenge einfache Ordnung.
2. (Gruppeneigenschaft) $(M,\circ)$ ist eine Gruppe.
3. (Monotonie) Für alle $m,n,p \in M$ gilt, dass
\begin{equation}
(m,n) \in R
\Leftrightarrow (m \circ p, n \circ p) \in R
\Leftrightarrow (p \circ m, p \circ n) \in R.
\end{equation}
4. (Archimedische Eigenschaft) Für alle $m,m' \in M$ gilt, dass wenn $(m,e)\in R$ gilt,
wobei $e \in M$ das neutrale Element der Gruppe $(M,\circ)$ bezeichnet, eine natürliche
Zahl $n$ existiert, so dass $(nm,m') \in R$ ist.
:::


:::{#exm-rgp-als-standardbeispiel-einer-archimedisch-geordneten-gruppe}
## $(\mathbb{R},>,+)$ als Standardbeispiel einer Archimedisch geordneten Gruppe
Wir haben oben bereits gesehen, dass $(\mathbb{R},>)$ eine strenge einfache Ordnung
ist und $(\mathbb{R},+)$ eine Gruppe ist. Die Monotonie von
$(\mathbb{R},>,+)$ folgt für $x,y,z \in \mathbb{R}$ aus
\begin{equation}
x > y  \Leftrightarrow x+z > y+z \Leftrightarrow z+x > z+y.
\end{equation}
Hinsichtlich der Archimedischen Eigenschaft gilt, dass $e := 0 \in \mathbb{R}$
das neutrale Element von $+$ auf $\mathbb{R}$ ist. Die Bedingung $(x,e)\in R$
entspricht also der Bedingung $x>0$ der Archimedischen Eigenschaft der reellen
Zahlen. Analog entspricht für $x,y \in \mathbb{R}$ die Aussage über die
Existenz eines  $n \in \mathbb{N}$ mit $(nm,m') \in R$ der Aussage über die
Existenz eines $n \in \mathbb{N}$ mit $nx > y$.
:::

:::{#thm-hölders-theorem}
## Hölders Theorem
$(M,R,\circ)$ sei eine Archimedisch geordnete Gruppe. Dann existiert eine Funktion
der Form
\begin{equation}
f : M \to \mathbb{R}, m \mapsto f(m)
\end{equation}
so dass für alle $m, n \in M$ gilt, dass
\begin{equation}
(m,n) \in R  \Leftrightarrow f(m) > f(n)
\end{equation}
und
\begin{equation}
f(m \circ n) = f(m) + f(n).
\end{equation}
:::

Für einen Beweis verweisen wir auf @krantz1971 (S. XXX). Wenn ein qualitiatives
Relationssystem $\mathcal{M}$ also eine Archimedisch geordnete Gruppe ist,
dann existiert ein Homomorphismus von $\mathcal{M}$ in das quantitative
Relationssystem $\mathcal{N} := (\mathbb{R},>,+)$. In anderen Worten ist eine
Archimedisch geordnete Gruppe also homomorph zu $(\mathbb{R},>,+)$.

### Extensive Strukturen {#sec-extensive-strukturen}

Als Modell des Messens von Massen überzeugt Hölders Theorem nicht ganz, insbesondere
deckt sich das Konzept der Archmidemisch geordneten Gruppe nicht vollständig mit
der Intuition zum Umgang mit Massen. Wir betrachten die Eigenschaften einer Archimedisch
geordneten Gruppe und die enstprechenden Intuitionen zum Umgang mit Massen.

\noindent (1) Die Tatsache, dass $(M,R)$ eine strenge einfache Ordnung ist, ist
vermutlich sinnvoll,allerdings kann es natürlich sein, dass zwei Objekte solch
ähnliche Masse haben, dass sie hinsichtlich der "ist schwerer als"  Relation
nicht unterscheidbar sind.

\noindent (2) Die Tatsache, dass $(M,\circ)$ eine Gruppe ist, ist intuitiv
weniger sinnvoll. Die Assoziativität der Kombination von Massen mag noch sinnvoll
sein, wenn man an das Kombinieren von Gewichten in einer Waagschale denkt. Auch
ein neutrales Element der Kombination von Massen mag als Idealisierung gerade noch
vorstellbar sein, ein inverses Element jedoch nicht: ein neutrales Element wäre
ein Objekt ohne Masse, ein zu einem Objekt $m$ inverses Objekt $n$ allerdings ein
Objekt, dass wenn mit $m$ kombiniert ein masseloses Objekt ergibt. Auf den Begriff
der Gruppe möchte man also nach Möglichkeit bei der Formulierung des
qualitativen Relationssystems bei Extensivmessungen verzichten.

\noindent (3) Die Monotonieeigenschaft bei der Bestimmung von Massen von Objekten
ist dagegen eher nachvollziehbar: Wenn ein Objekt $m$ schwerer als ein Objekt
$n$ eingeschätzt wird, dann sollten auch die Kombination von $m$ mit einem dritten
Objekt schwerer eingschätzt werden als die Kombination von $n$ mit eben diesem Objekt.

\noindent (4)  Die Archimedische Eigenschaft ist zumindest als Idealisierung vorstellbar:
für zwei nicht masselose Objekt $m$ und $p$ ist es vorstellbar, ein Objekt $nm$ aus
$n$ Kopien von $m$ zu erstellen, so dass $m$ schwerer als $p$ ist.

Basierend auf den oben aufgeführten Unzulänglichkeiten des Modells der
Extensivmessung nach Hölder, insbesondere der intuitiven Nicht-Existenz inverser
Objekte, haben verschiedene Autoren Modifikationen des repräsentationstheoretischen
Modells der Extensivmessung vorgeschlagen. Wir betrachten im Folgenden
speziell das Modell der Extensivmessung nach @roberts1968. Für eine kritische
Diskussion dieses Ansatzes, siehe @michell1996, @michell1997 und @michell2021.

:::{#def-extensive-struktur}
## Extensive Struktur
Ein Relationssystem $(M,R,\circ)$ heißt *extensive Struktur*, wenn
$(M,R,\circ)$ folgende Eigenschaften hat:

1. (Strenge schwache Ordnung) $R$ ist eine strenge schwache Ordnung.
2. (Schwache Assoziativität) Für alle $m,n,p\in M$ gilt
\begin{equation}
m \circ (n \circ p) \sim (m \circ n) \circ p
\end{equation}
wobei  $m \sim n$ für alle $m,n \in M$ genau dann gilt, wenn weder $(m,n)\in R$ noch $(n,m)\in R$.
3. (Monotonie) Für alle $m,n,p\in M$ gilt, dass
\begin{equation}
(m,n) \in R \Leftrightarrow (m \circ p, n \circ p)\in R \Leftrightarrow (p \circ m,p \circ n)\in R.
\end{equation}
4. (Archimedische Eigenschaft) Für alle $p,q,r,s\in M$ gilt, dass wenn $(p,q)\in R$
gilt, ein $n \in \mathbb{N}$ existiert, so dass $(np \circ r, nq \circ s) \in R$.
:::

Im Vergleich zu einer Archimedisch geordneten Gruppe ist nur die Monotonieeigenschaft
unverändert. Der Begriff der Gruppe taucht als definierende Eigenschaft nicht auf.
Wir erläutern im Folgenden die Eigenschaften (1), (2) und (4) einer extensiven Struktur.

Im Unterschied zur Archimedisch geordneten Gruppe fordert Eigenschaft (1) lediglich,
das $(M,R)$ eine strenge schwache Ordnung und keine strenge einfache Ordnung ist. Im Gegensatz
zur strengen einfachen Ordnung muss eine strenge schwache Ordnung nicht vollständig sein. Es muss
also nicht für alle $m,n$ mit $m \neq n$ gelten, dass $(m,n) \in R$ oder $(n,m) \in R$. In Bezug
auf die "ist schwerer als" Relation bedeutet dies, dass im Modell der extensiven Struktur im Gegensatz
zum Modell der Archimedisch geordneten Grupppe zwei ungleiche Objekte durchaus die
gleiche Masse haben können.

Eigenschaft (2) fordert im Gegensatz zur Assoziativität lediglich, dass für
$r := m \circ (n \circ p)$ und $s := (m \circ n) \circ p$ gilt, dass $(r,s) \notin R$
und dass $(s,r) \notin R$, dass also zum Beispiel in Bezug auf das Messen einer
Masse $r$ nicht schwerer als $s$ und $s$ nicht schwerer als $r$ ist. Es ist
aber nicht gefordert, dass $r = s$ ist, dass $r$ und $s$ also identisch sind.
Scheinbar spielt die schwache Assoziativität weiter auf die Unterscheidung von
Zahlen, die wenn sie nicht verschieden sind, identisch sind, und qualitativen
Objekten, die wenn sie die gleiche Eigenschaft haben, nicht identisch sein müssen,
an. Klare Unterscheidungen des "Messens von Objekten" und des "Messens von Attributen"
von Objekten finden sich bei @mundy1987 und @swoyer1987.

Eigenschaft (4) ist eine notwendige Bedingung für die Existenz eines Homomorphismus
von einer extensiven Struktur nach $(\mathbb{R},>,+)$ und reflektiert die
Archimedische Eigenschaft der reellen Zahlen. Es existiere also ein Homomorphismus von
$(M,R,\circ)$ nach $(\mathbb{R},>,+)$. Dann impliziert $(p,q)\in R$, dass $f(p) > f(q)$
und damit $f(p) - f(q)>0$. Mit der Archimedischen Eigenschaft der reellen Zahlen
existiert dann ein $n \in \mathbb{N}$, so dass $n(f(p)-f(q))>f(s)-f(r)$ für
alle $r,s\in M$. Dann aber gilt
\begin{align}
\begin{split}
n(f(p)-f(q))>f(s)-f(r) & \Leftrightarrow nf(p)-nf(q) > f(s) - f(r)   \\
                       & \Leftrightarrow nf(p) + f(r) > nf(q) + f(s) \\
                       & \Leftrightarrow f(np + r) > f(nq + s) \Leftrightarrow (np \circ r, nq \circ s) \in R.
\end{split}
\end{align}


### Repräsentation und Eindeutigkeit {#sec-repräsentation-und-eindeutigkeit}

:::{#thm-repräsentation-extensiver-strukturen}
## Repräsentation extensiver Strukturen
$\mathcal{M} := (M,R,\circ)$ sei ein qualitatives Relationssystem aus einer Menge
$M$, einer Binärrelation $R$ und einer Operation $\circ$ und $\mathcal{N} := (\mathbb{R},>,+)$
sei ein numerisches Relationssystem. Dann existiert ein Homomorphismus $f$
von $\mathcal{M}$ nach $\mathcal{N}$, also eine Funktion der Form
\begin{equation}
f : M \to \mathbb{R}, m  \mapsto f(m)
\end{equation}
mit den Eigenschaften
\begin{equation}
(m,n) \in R \Leftrightarrow f(m) > f(n)
\end{equation}
und
\begin{equation}
f(m \circ n) = f(m) + f(n)
\end{equation}
dann und nur dann, wenn $(M,R,\circ)$ eine extensive Struktur ist.
:::

Wir verzichen auf einen Beweis und verweisen auf @roberts1968 oder @rossi2006.

:::{#thm-eindeutigkeit-der-repräsentation-extensiver-strukturen}
## Eindeutigkeit der Repräsentation extensiver Strukturen
Das qualitative Relationssystem $\mathcal{M} := (M,R,\circ)$ sei eine extensive
Struktur, $\mathcal{N}:= (\mathbb{R},>,+)$ sei ein numerisches Relationssystem
und es sei $f \in \mathcal{H}(\mathcal{M}, \mathcal{N})$. Dann ist
$(\mathcal{M},\mathcal{N}, f)$ eine Verhältnisskala.
:::

:::{.proof}
Für einen vollständigen Beweis müssten wir nachweisen, dass (1) die Repräsentation
regulär ist, (2.1) aus der Tatsache,dass $\phi$ eine Ähnlichkeitstransformation ist,
folgt, dass $\phi \circ f \in \mathcal{H}(\mathcal{M},\mathcal{N})$ und (2.2),
dass aus $\phi \circ f \in \mathcal{H}(\mathcal{M},\mathcal{N})$ folgt, dass $\phi$
eine Ähnlichkeitstransformation ist. Für die Skizze eines solchen vollständigen Beweis
verweisen auf @roberts1984, S. 130. Wir begnügen uns hier mit dem Nachweis von (2.1).

*$\phi$ ist eine Ähnlichkeitstransformation $\Rightarrow \phi \circ f \in \mathcal{H}(\mathcal{M},\mathcal{N})$*

Wir wollen zeigen, dass wenn $\phi$ von der Form
\begin{equation}
\phi : f(M) \to \mathbb{R}, x \mapsto \phi(x) := \alpha x \mbox{ mit } \alpha > 0
\end{equation}
ist, die Verkettung von $\phi$ mit dem Homomorphismus $f$ wiederum ein Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$ ist.
Dazu halten wir fest, dass einerseits gilt, dass
\begin{equation*}
(m,n) \in R \Leftrightarrow f(m) > f(n)  \Leftrightarrow \alpha f(m) > \alpha f(n) \Leftrightarrow \phi(f(m)) > \phi(f(n)) \Leftrightarrow (\phi \circ f)(m) > (\phi \circ f)(n)
\end{equation*}
und dass andererseits gilt, dass
\begin{align}
\begin{split}
(\phi \circ f)(m \circ n)
& = \phi(f(m \circ n))                      \\
& = \phi(f(m) + f(n))                      \\
& = \alpha(f(m) + f(n))                    \\
& = \alpha f(m) + \alpha f(n)              \\
& = \phi(f(m))  + \phi(f(n))                \\
& = (\phi \circ f)(m) + (\phi \circ f)(n).
\end{split}
\end{align}
Damit erfüllt $\phi \circ f$ aber die definierenden Eigenschaften eines Homomorphismus von $\mathcal{M}$ nach $\mathcal{N}$.
:::

Die Klasse der zulässigen Transformationen von $f$ sind also die Ähnlichkeitstransformationen.
Extensive Strukturen sind also nur bis auf eine Ähnlichkeitstransformation
eindeutig repräsentiert.


## Repräsentation additiv-konjunkter Strukturen{#sec-repräsentation-additiv-konjunkter-strukturen}

\footnotesize
## Selbstkontrollfragen {#sec-selbstkontrollfragen}

1. Erläutern Sie die Grundidee des Messens.
1. Erläutern Sie das Messen von Entscheidungsoptionspräferenzen.
1. Erläutern Sie das Messen der Masse von Objekten.
1. Definieren Sie den Begriff der Binärrelation.
1. Definieren Sie den Begriff einer dreistelligen Relation.
1. Erläutern Sie die $\ge$ Relation.
1. Definieren Sie den Begriff der reflexiven Binrärelation.
1. Definieren Sie den Begriff der irreflexiven Binrärelation.
1. Was ist der Unterschied zwischen einer nicht reflexiven und einer irreflexiven Relation?
1. Definieren Sie den Begriff einer symmetrischen Binärrelation.
1. Definieren Sie den Begriff einer transitiven Binärrelation.
1. Definieren Sie den Begriff einer negativ-transitiven Binärrelation.
1. Geben Sie die Relationsdefinition einer Funktion wieder.
1. Definieren Sie den Begriff der Operation.
1. Definieren Sie den Begriff des Relationssystems.
1. Geben Sie ein Beispiel für ein qualitatives Relationssytem.
1. Geben Sie ein Beispiel für ein numerisches Relationssystem.
1. Geben Sie die Definition eines Homomorphismus wieder.
1. Geben Sie die Definition eines Isomorphismus wieder.
1. Geben Sie die Definition einer Skala wieder.
1. Definieren Sie das Repräsentationsproblem der Repräsentationstheorie.
1. Erläutern Sie die Begriffe der notwendigen und hinreichenden Eigenschaften eines qualitativen Relationssystems für die Existenz eine Homomorphismus.
1. Definieren Sie das Eindeutigkeitsproblem der Repräsentationstheorie.
1. Definieren den repräsentationstheoretischen Begriff der Bedeutsamkeit.
1. Warum ist die Aussage "Objekt $m$ ist doppelt so schwer wie Objekt $n$" intuitiv bedeutsam, die Aussage "Objekt $m$ ist doppelt so warm wie Objekt $n$" dagegehn intuitiv nicht bedeutsam?
1. Definieren Sie den Begriff der zulässigen Transformation einer Skala.
1. Definieren Sie die Begriffe der regulären Skala und der regulären Repräsentation.
1. Definieren Sie den Begriff der Skalenart.
1. Definieren Sie den Begriff der Ordinalskala.
1. Definieren Sie den Begriff der Verhältnisskala.
1. Definieren Sie den Begriff der Intervallskala.
1. Geben Sie das Theorem zur Skalenart wieder.
1. Definieren Sie den repräsentationstheoretischen Begriff der Bedeutsamen Aussage.
1. Geben Sie das Theorem zur Bedeutsamkeit von Größenvergleichen wieder und erläutern Sie es.
1. Geben Sie das Theorem zur Bedeutsamkeit von Größenvergleichen von Differenzen wieder und erläutern Sie es.
1. Was bedeutet es, dass eine Aussage bei einer gegebenen Skalenart "im Allgemeinen" nicht bedeutsam ist?
1. Geben Sie das Theorem zur Bedeutsamkeit von Verhältnisaussagen wieder und erläutern Sie es.
1. Definieren Sie den Begriff der reellwertigen Statistik vor dem Hintergrund einer Skala $(\mathcal{M}, \mathcal{N},f)$.
1. Definieren Sie den Begriff der bedeutsamen Statistik.
1. Geben Sie das Theorem zur Bedeutsamkeit der Summe wieder und erläutern Sie es.
1. Geben Sie das Theorem zur Bedeutsamkeit des Mittelwerts wieder und erläutern Sie es.
1. Erläutern Sie zwei prinzipielle Vorgehensweisen bei repräsentationstheoretischen Überlegungen.
1. Erläutern Sie den Begriff der Ordungsrelation.
1. Erläutern Sie das Ziel und den wissenschaftlichen Gewinn von Ordinalmessungen.
1. Definieren Sie den Begriff der asymmetrischen Binärrelation.
1. Definieren Sie den Begriff der negativ transitiven Binärrelation.
1. Definieren Sie den Begriff der strengen schwachen Ordnung.
1. Geben Sie das Theorem zur Irreflexivität und Transitivität strenger schwacher Ordnungen wieder.
1. Geben Sie das Theorem zur Repräsentation strenger schwacher Ordnungen wieder.
1. Geben Sie das Theorem zur Eindeutigkeit strenger schwacher Ordnungen wieder.
1. Erläutern Sie die Konstruktion eines Homomorphismus von einer strengen schwachen Ordnung nach $(\mathbb{R},>)$ anhand eines Beispiels aus dem Bereich der Präferenzmessung.
1. Erläutern Sie das zentrale Charakteristikum der Extensivmessung.
1. Geben Sie jeweils drei Beispiele für Extensivmessungen in der Physik und in der Psychologie.
1. Definieren Sie den Begriff der strengen einfachen Ordnung.
1. Definieren Sie den Begriff der Gruppe.
1. Geben  Sie das Theorem zur Archimedischen Eigenschaft der reellen Zahlen wieder.
1. Definieren Sie den Begriff der Archimedisch geordneten Gruppe.
1. Geben Sie Hölders Theorem wieder.
1. Erläutern Sie die Motivation zur Formulierung des Begriffs der extensiven Struktur.
1. Gegeben Sie das Theorem zur Repräsentation extensiver Strukturen an.
1. Geben Sie das Theorem zur Repräsentation extensiver Strukturen wieder. -->

\normalsize